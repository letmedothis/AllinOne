<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allinone.supply.mapper.SupplyUserMapper">
    <select id="selectRankLevel" resultType="String">select rank_level from sys_user where user_id=#{userId} and del_flag='0'</select>
</mapper>
