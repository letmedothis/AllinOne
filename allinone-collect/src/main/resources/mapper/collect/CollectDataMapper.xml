<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allinone.collect.mapper.CollectDataMapper">

    <resultMap type="CollectData" id="CollectDataResult">
        <id property="dataId" column="data_id"/>
        <result property="templateId" column="template_id"/>
        <result property="formData" column="form_data"/>
        <result property="bizStatus" column="biz_status"/>
        <result property="deptId" column="dept_id"/>
        <result property="flowInstanceId" column="flow_instance_id"/>
        <result property="dataCode" column="data_code"/>
        <result property="version" column="version"/>
        <result property="submitBy" column="submit_by"/>
        <result property="submitTime" column="submit_time"/>
        <result property="remark" column="remark"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectCollectDataVo">
        select data_id, template_id, biz_status, dept_id, data_code, version, submit_by, submit_time, remark, del_flag, create_by, create_time, update_by, update_time
        from collect_data
    </sql>

    <select id="selectCollectDataList" parameterType="CollectData" resultMap="CollectDataResult">
        <include refid="selectCollectDataVo"/>
        where del_flag = '0'
        <if test="templateId != null"> and template_id = #{templateId}</if>
        <if test="bizStatus != null and bizStatus != ''"> and biz_status = #{bizStatus}</if>
        <if test="submitBy != null and submitBy != ''"> and submit_by = #{submitBy}</if>
        order by create_time desc
    </select>

    <select id="selectCollectDataById" parameterType="Long" resultMap="CollectDataResult">
        select data_id, template_id, form_data, biz_status, dept_id, data_code, version, submit_by, submit_time, remark, del_flag, create_by, create_time, update_by, update_time
        from collect_data where data_id = #{dataId}
    </select>

    <insert id="insertCollectData" parameterType="CollectData">
        insert into collect_data (data_id, template_id, form_data, biz_status, dept_id, data_code, version, remark, del_flag, create_by, create_time, submit_by)
        values (#{dataId}, #{templateId}, #{formData}, #{bizStatus}, #{deptId}, #{dataCode}, #{version}, #{remark}, '0', #{createBy}, #{createTime}, #{submitBy})
    </insert>

    <update id="updateCollectData" parameterType="CollectData">
        update collect_data
        <set>
            <if test="formData != null">form_data = #{formData},</if>
            <if test="bizStatus != null">biz_status = #{bizStatus},</if>
            <if test="dataCode != null">data_code = #{dataCode},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="version != null">version = version + 1,</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = #{updateTime}
        </set>
        where data_id = #{dataId}
    </update>

    <update id="updateCollectDataStatus" parameterType="CollectData">
        update collect_data set biz_status = #{bizStatus}, submit_by = #{submitBy}, submit_time = #{submitTime},
            version = version + 1, update_time = #{updateTime}
        where data_id = #{dataId}
    </update>

    <update id="deleteCollectDataById" parameterType="Long">
        update collect_data set del_flag = '2' where data_id = #{dataId}
    </update>

    <update id="deleteCollectDataByIds" parameterType="Long">
        update collect_data set del_flag = '2' where data_id in
        <foreach item="dataId" collection="array" open="(" separator="," close=")">#{dataId}</foreach>
    </update>

</mapper>

