# 供应链采购审批与票据台账系统 —— V2.0 需求与设计(交付口径修订版)

> **定位**:本文是 `供应链采购审批与票据台账系统_详细设计_V1.0.md` 的升级版,落实真实企业交付口径。
> 凡本文明确**修订/新增**之处,以本文为准;本文未涉及的状态机、数值口径、审计、事务并发等细节,继续沿用 V1.0 对应章节(1.3/4/6/8/11/13/14/15/16/17)。
> 基线(均已合回 main):Flowable 引擎修复 `3353d06`、M2 职级数据范围 `c0d72d5`、M3 数电票解析 `71c1cf0`、M4 付款核销 `7500115`(当前 main HEAD)。

---

## 1. 版本范围变更(相对 V1.0)

| 主题 | V1.0 | V2.0 |
| --- | --- | --- |
| 付款/应付核销 | 1.2 明确排除 | **纳入交付**(新增模块,§5) |
| 发票来源 | 仅约定 `DemoInvoice schemaVersion=1.0` | **真实数电票 XML 解析**,PDF/OFD 仅作附件(§6) |
| 红字发票/进项勾选/税务验真 | 排除 | **继续排除**,留后续版本 |
| 组织 | 演示角色(经办/采购员/主管/仓管/财务/管理员) | **采购部 / 财务部 / 信息部 / 仓储部** 四部门 + 职级三级(§2) |
| 数据范围 | 按角色(全局/本人/分配) | **按职级**:总监看全部、部门领导看本部门、职员看本人(§3) |
| 审批引擎 | 应用内固定流程 | 采购订单走 **Flowable 引擎**;供应商/发票/付款走固定流程(§4) |

---

## 2. 组织、角色与权限

### 2.1 部门与岗位

| 部门 | 岗位(职级) | 职责 |
| --- | --- | --- |
| 采购部 | 采购总监(总监)、采购经理(部门领导)、采购职员(职员) | 创建供应商/订单/付款申请;订单提交 |
| 财务部 | 财务总监(总监)、财务经理(部门领导)、财务职员(职员) | 发票财务审核、付款复核、付款终审(按金额分档) |
| 仓储部 | 仓储负责人(部门领导)、仓管员(职员) | 分批收货/入库确认 |
| 信息部 | 系统管理员(职员,但数据范围=全部) | 系统维护、参数/菜单/用户管理,不参与业务审批 |

### 2.2 职级承载方式

- 建议在 `sys_user` 增加扩展字段 `rank_level`(`EXEC`/`LEADER`/`STAFF`)或在"岗位"上承载职级;数据范围由 `(部门, 职级)` 计算,不写死在角色上。
- 历史审批与审计保留"提交/审批时"的用户姓名与角色快照,职级变更不影响已生成单据与任务。

### 2.3 操作权限矩阵(修订 V1.0 3.2)

| 操作 | 采购职员 | 采购经理/总监 | 财务职员/经理 | 财务总监 | 仓储负责人/仓管 | 信息管理员 |
| --- | --- | --- | --- | --- | --- | --- |
| 供应商/订单创建 | 是 | 是 | 否 | 否 | 否 | 是 |
| 编辑/提交本人草稿与退回单 | 本人 | 本人 | 否 | 否 | 否 | 本人 |
| 供应商/订单审批 | 否 | 指定(采购主管=经理/总监) | 否 | 否 | 否 | 否 |
| 分批入库确认 | 否 | 否 | 否 | 否 | 是 | 是 |
| 发票采购审核 | 指定采购员 | 否 | 否 | 否 | 否 | 否 |
| 发票财务审核 | 否 | 否 | 指定 | 指定 | 否 | 否 |
| 付款申请 | 是 | 是 | 否 | 否 | 否 | 否 |
| 付款复核/终审 | 否 | 否 | 复核(财务经理/职员) | 终审(金额分档) | 否 | 否 |
| 台账/导出 | 本人范围 | 本部门/全部 | 全部 | 全部 | 仓储范围 | 全部 |

> 审批任务不受数据范围限制:被指派的审批人必须能看到对应单据。

---

## 3. 数据范围(按职级,修订 V1.0 3.3)

### 3.1 通用规则

| 职级 | 可见数据 |
| --- | --- |
| 总监 | 全部业务单据与台账(采购/财务/仓储/信息各域按"总监看所有") |
| 部门领导 | 本部门所有(不含下级部门;将来增设子部门时再升级为"本部门及以下") |
| 职员 | 本人创建/被指派的单据 |

规则适用于列表、详情、搜索建议、统计、导出、附件下载;无权限记录不可通过猜 ID 访问。

### 3.2 角色例外

- **仓管员(仓储部职员)**:收货作业需读取**已批准采购订单**的必要字段(单号/供应商/行/数量/预计到货)与全部入库记录;不可见发票、付款与完整供应商银行账号资料。
- **审批人**:被指派任务可见对应单据,与职级范围无关。
- **信息管理员**:数据范围=全部,但只做运维操作。
- **财务职员(STAFF)**:通过"审批任务例外"查看被指派的单据;台账/对账的"全部"读仅授予财务经理(LEADER)及以上;若某财务职员需跨部门日常对账,应设为 LEADER 或另行授予白名单例外(2026-09 决策,与 §2.3 财务"全部"口径的调和方案)。
- **仓储收货读取(目的受限视图)**:仓管通过受权接口 `POST /supply/receipts/prepare|confirm`(权限 `supply:receipt:add`)读取已批准订单必要字段并确认入库,按权限而非职级范围放行;订单选择器 UI 属后续增强。

### 3.3 实现要点

- 列表/详情/统计查询统一按 `(dept_id, rank_level, creator_id/assignee)` 拼装可见条件;
- 供应商选择器只返回完成选择所需的字段(名称、税号、准入状态),不暴露银行账号与资质附件。

---

## 4. 审批引擎(修订 V1.0 4.3/10)

| 单据 | 引擎 | 说明 |
| --- | --- | --- |
| 采购订单 | **Flowable**(`purchase-order-approval`) | 提交→主管审批;`documents.workflow_engine='FLOWABLE'`;支持 BPMN 设计器调整审批人类型 |
| 供应商准入 | 内置固定流程 | 提交→主管审批(经理/总监候选人) |
| 发票双审 | 内置固定流程 | 提交→采购审核→财务审核 |
| 付款核销 | 内置固定流程 | 采购申请→财务复核→财务总监终审(金额分档,§5.3) |

- 引擎归属以 `documents.workflow_engine`(`LEGACY`/`FLOWABLE`)为唯一事实源;撤回/作废据此同步取消 Flowable 运行实例,避免幽灵待办。
- 历史在途 `LEGACY` 采购订单仍由旧"审批中心"处理;退回后重新提交进入 Flowable。
- 迁移期约束(沿用 V1.0 10.3):同一单据不得同时由内置流程与引擎驱动。

---

## 5. 付款 / 应付核销模块(新增)

### 5.1 应付来源与余额

- 发票**财务审核通过**后计入应付(按发票,可按供应商汇总);作废发票移出应付并释放占用。
- 应付余额 = 票面价税合计 − 已核销金额;每张发票/每个供应商维度可查可核销余额。

### 5.2 付款单

| 字段 | 说明 |
| --- | --- |
| 单号 | `PAY-YYYYMMDD-00001`(沿用序列规则) |
| 供应商 | 已准入供应商 |
| 申请金额 | 本单付款总额(人民币分) |
| 核销明细 | 多行:关联发票 + 核销金额;核销金额合计 = 申请金额;单发票累计核销 ≤ 应付余额,禁止超额、允许部分核销 |
| 状态 | DRAFT 草稿 / IN_REVIEW 审批中 / APPROVED 已付款(完成核销)/ RETURNED 退回 / VOID 作废 |
| 防重 | 幂等键(供应商 + 申请金额 + 核销组合 + 业务时间窗);禁止对同一发票超额/重复核销 |

### 5.3 审批链与金额分档(已确认)

`采购部经办申请 → 财务部复核(≤10 万元直接通过)→ 财务总监终审(单笔 >10 万元)`
- 复核岗位:财务经理或财务部授权职员;
- 分档阈值:**单笔 10 万元**;超阈值必须财务总监终审。

### 5.4 状态机与不变量

- 草稿 →(提交)→ 审批中 →(复核通过/总监终审)→ 已付款(核销生效);退回/撤回释放核销占用。
- "审批通过 ≠ 付款"在付款模块升级为:**付款单生效 = 实际付款动作 + 应付核销**。
- 作废已付款单需管理员 + 财务角色双确认,且只允许在冲正流程(后续版本)前保留审计记录。
- 银行流水导入、回单 OCR、跨币种不进本版(留接口)。

### 5.5 台账联动

- 应付账龄:未核销金额按发票开票日期/到期日展示;
- 追溯链:付款 → 发票 → 订单 → 入库行,全链可查。

### 5.6 数据模型增量(示意)

| 表 | 关键字段(实现为准) |
| --- | --- |
| `payments` | id、number、supplier_id、amount_cents、threshold_exceeded、status、current_node、creator_id、creation_key、revision、remark、review_comment、director_comment |
| `payment_lines` | id、payment_id、invoice_id、allocated_cents |
| `ap_balances` | 未建物化表;应付余额由查询按「票面 − 已核销/在途占用」实时计算 |

> 台账只读业务事实与分配记录,不另设手工录入数据源(沿用 V1.0 10.2 原则)。

---

## 6. 发票模块:真实数电票(修订 V1.0 7)

### 6.1 范围

- 解析对象:符合税务机关数电票规范的 **XML**(蓝字);PDF/OFD 原样上传存附件,不解析内嵌数据。
- 红字(负数/冲红)发票、进项勾选、税务验真、OCR **不在 V2.0**。

### 6.2 关键映射与校验

- 票号:数电票号码(20 位),保留前导零,存 `invoice_number varchar(32)`;唯一键 = 销方税号 + 票号(沿用 V1.0 D05)。
- 票面要素:销方/购方名称税号、开票日期、货物或应税劳务清单行、未税金额/税额/价税合计;行级关联到订单行,提交时按入库批次自动分配。
- 解析结果**人工确认**后才可提交;单价/税率/单位差异说明必填(V1.0 D10);解析失败保留原件人工录入,界面不显示"验真通过"。
- 金额口径沿用 V1.0 6.3(行未税=数量×未税单价四舍五入到分等)。

### 6.3 交付方式

- 以**真实脱敏票样**驱动解析器开发与回归;保留 `DemoInvoice` 解析分支仅用于演示。

---

## 7. 数据模型增量汇总(相对 V1.0 11)

| 变更 | 说明 |
| --- | --- |
| `documents.workflow_engine` | varchar(16) NOT NULL DEFAULT 'LEGACY',标识审批引擎归属 |
| `payments` / `payment_lines` | 付款与核销明细(§5.6) |
| `sys_user.rank_level`(或岗位承载) | 职级三级,支撑 §3 数据范围 |

---

## 8. 接口增量(相对 V1.0 12;实现为准)

| 接口 | 说明 |
| --- | --- |
| `GET/POST/PUT /supply/payments` | 付款单列表/创建/编辑 |
| `GET /supply/payments/supplier-options` | 可付款的已准入供应商下拉 |
| `POST /supply/payments/{id}/submit` | 付款申请提交(服务端校验余额+分档) |
| `POST /supply/payments/{id}/review` | 财务复核(通过→≤10 万 APPROVED / >10 万转总监;退回) |
| `POST /supply/payments/{id}/director-decision` | 财务总监终审(通过→APPROVED / 退回) |
| `POST /supply/payments/{id}/void` | 作废(草稿/退回/审批中,创建人或管理员) |
| `GET /supply/payments/ap-balance` | 应付余额(按发票,票面−已核销/在途占用;按开票日期排序,可用作账龄) |
| `POST /supply/invoices/parse-xml` | 发票 XML 解析入口(路由 DemoInvoice / 数电票蓝字) |

> 说明(与早期草案的差异):未另建 `/supply/ledger/ap-aging`,账龄以 `ap-balance` 的开票日期排序承担;付款审批在付款模块内闭环(未并入统一审批中心),复核/终审接口分离为 review / director-decision。职级数据范围在既有查询接口上按 §3 落列,不新增独立接口。

---

## 9. 里程碑与验收(更新)

| 里程碑 | 内容 | 状态 |
| --- | --- | --- |
| M0 | 在途 WIP 基线整理 | ✅ 已合回(`2e9c388`…`357c393`) |
| M1 | V2.0 口径评审 | ✅ 业务方签认(§10),出口达成 |
| M2 | 组织/职级/数据范围落地 | ✅ 已合回 `c0d72d5`(`sys_user.rank_level` + 解析器 + 供应商/订单/发票/台账/工作台按职级) |
| M3 | 发票真实数电票解析重做 | ✅ 已合回 `71c1cf0`(按真实票样结构校准,掩码夹具回归;多行票样待补) |
| M4 | 付款/应付核销实现 | ✅ 已合回 `7500115`(B1–B4;编译/联调待 CI) |
| M5 | CI 构建 + MySQL 冒烟 | ⏳ 本机按用户决定跳过;待有网环境执行 `mvn -pl allinone-supply -am test` 与联调 |
| M6 | 可靠性/并发/17 章 + 新增验收、试运行 | ⏳ 待做 |

> 说明:全量代码(M0–M4)**尚未经真实编译/运行验证**(本环境无网络,M5 被要求跳过);静态校验(XML 良构、mapper 接口↔语句对齐、diff --check)在各批均通过。M5 的有网编译与 MySQL 冒烟是进入 M6 前的首个硬门禁。

---

## 10. 已确认决策(业务方签认,2026-09)

| # | 问题 | 决策 |
| --- | --- | --- |
| 1 | 付款分档 | 阈值 10 万元;复核岗=财务经理/授权职员,超阈值财务总监终审(§5.3) |
| 2 | 仓储部 | 负责人(部门领导)+ 仓管员(职员);仓管仅见已批准待收货订单必要字段,不可见发票/付款(§2.1/§3.2) |
| 3 | 历史数据 | 不迁移存量业务数据,上线从零建档;期初应付按少量发票人工补录 |
| 4 | 账套 | 单公司单账套,沿用 company_profile 单行与公司税号锁定规则 |
| 5 | 单据量 | 中小规模(日均数十笔、低并发),现有乐观锁+事务设计够用,验收阶段补负载抽查 |

以上签认后,不再有 V1.0 遗留口径级待确认项;进入 M2(组织/职级/数据范围落地)可直接开工。

---

## 11. 收尾:设计 → 实现对照与遗留清单

### 11.1 设计要点 → 实现落地(均在 main)

| 设计要点 | 实现(文件/提交) |
| --- | --- |
| §2 组织/职级;§3 数据范围 | `sys_user.rank_level`(幂等 ALTER)+ `SupplyDataScopeResolver` + 供应商/订单/发票/台账/工作台按 `mode(ALL/DEPT/SELF)` 过滤;演示赋职级脚本 `sql/supply_org_rank_seed.sql`(M2,`c0d72d5`;设计清单 `doc/M2_组织职级数据范围_改动点清单.md`) |
| §4 采购订单走 Flowable | `documents.workflow_engine` 归属 + 撤回/作废同步取消实例(S1–S4 修复,`3353d06`) |
| §6 数电票(蓝字)解析 | `InvoiceXmlParser` 路由 + `DigitalInvoiceXmlParser`(真实票样结构校准,20 位票号)+ 夹具 `digital-invoice-01.fixture.xml`;契约 `doc/M3_数电票解析_契约.md`(M3,`71c1cf0`) |
| §5 付款/应付核销 | `payments/payment_lines` + `PaymentController/ServiceImpl/Mapper` + 前端 `views/supply/payment/index.vue`;设计 `doc/M4_付款应付核销_设计.md`(M4,`7500115`) |

### 11.2 已知实现差异(有意为之,以上文为准)

- 付款审批在**付款模块内闭环**(review/director-decision),未并入旧"审批中心"或 Flowable;复核/终审人身份与时间暂未落库(待审计扩展);
- 未建 `ap_balances` 物化表与独立 `/ledger/ap-aging` 接口,应付余额与账龄由 `ap-balance`(开票日期序)承担;
- 财务用户在付款模块内可见全部(便于复核);职级范围在其它单据模块按 §3 生效;
- 数电票解析按**真实票样结构**实现,`SignatureValue` 若含未转义 `&` 的真实文件需在导入侧处理(当前解析器按标准 XML 拒收)。

### 11.3 遗留清单(收尾后待办)

1. **M5**:有网环境 `mvn -pl allinone-supply -am test` + 前端构建 + MySQL 冒烟(M0–M4 代码均未真实编译);
2. 数电票**多行清单**真实票样 → 补契约 §5 项 2 回归;
3. 付款复核/终审人身份与时间落库(审计)、付款 Excel 导出、统一待办接入(可选);
4. M6:V1.0 §17 验收 + 新增用例、并发/余额竞争验证、试运行与培训;
5. 组织初始化:真实企业按 §2/§3 在系统内维护部门与 `rank_level`(演示可用 `sql/supply_org_rank_seed.sql`)。
