# AllinOne 静态审查后续修复计划

## 第一阶段：恢复构建（P0，必须最先做）
1. 修复 `WorkReportController.java:256` 的 `currentUserId` 未定义（恢复声明 `Long currentUserId = SecurityUtils.getUserId();` 或改用 `SecurityUtils.getUserId()`）
2. 修复 `WorkReportAccessService.java:152` 的 `LoginUser.getRoles()` 不存在（改为 `SecurityUtils.getLoginUser().getUser().getRoles()` 取 `List<SysRole>` 的 roleKey，或改用 permissions 判断管理员）
3. 运行 `mvn compile -pl allinone-collect -am`（或全量 `mvn compile`）验证编译通过

## 第二阶段：修复高危可用性问题
4. **24 小时强制踢线**：`AuthzVersionService` 版本 key 去掉短 TTL（改为与登录会话同生命周期，或 verifyToken 时惰性续期）
5. **裸 500**：`TokenService.verifyToken` 版本不匹配改为抛 `AuthenticationException`（如 `ServiceException`/自定义认证异常），使 Spring Security 走 401 通道；同时处理 `authzVersion` 为 null 的旧会话（拆箱 NPE）
6. **30 分钟硬过期**：`refreshToken` 滑动续期时重签 JWT，或恢复 RuoYi 原有的“Redis TTL 即会话有效期、JWT 不设 exp”语义（取后者，改动最小且是原设计）
7. **角色级 Sheet 授权**：补全 `hasExplicitSheetPermission` 的 role 分支（按 permType="role" 匹配当前用户角色）
8. **dataScope 变更不刷新**：`SysRoleController.dataScope()` 调用版本递增
9. Jimu 票据：最小处理——移除死代码或在文档中标注现状（视改动风险决定，不强行接通）

## 第三阶段：修复中危问题
10. `CollectDataCellMapper.xml:24` 的 `select * + resultType` 改为显式列 + resultMap；`:21` 的 `update_time = values(create_time)` 改正
11. `CollectFieldMappingMapper.java` 删除 XML 中不存在的 `batchUpsert`/`deleteCollectFieldMappingByTemplate` 声明（或补 XML）
12. `WorkReportSheetServiceImpl` 单个删除补 `@Transactional`
13. 写回白名单默认值从空改为拒绝所有时给出更明确的配置提示（或按 application.yml 注释说明）

## 第四阶段：修复文档（按文档操作能跑起来）
14. README：初始化 SQL 表补 `allinone_biz_update.sql`、`allinone_menu.sql`；补 `JWT_SECRET`/`DB_PASSWORD` 环境变量说明；修正 admin 账号说明（需设置 `@bootstrap_password_bcrypt` 或提供激活步骤）；修正 `-P production` 不存在的 profile
15. SQL_EXECUTION_ORDER.md：补 ry_20260417.sql 需以 SOURCE 方式导入以设置 bootstrap 密码变量的说明

## 第五阶段：验证与产出
16. `mvn compile` 全量编译验证
17. 将本轮发现与修复汇总为新报告写入 `doc/`（含回归验证结论：原 93 条中哪些真正修好、哪些未修完）
18. 提交 commit（fix + docs 分开）

## 不做的事
- 不在本轮修复约 20 个前端 loading 挂起页面（量大且非阻断，列为后续任务清单写入报告）
- 不强行接通 Jimu 票据全链路（涉及前端 iframe 改造，单独评估）