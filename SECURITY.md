# 安全说明

## 报告漏洞

请通过 GitHub 仓库的 Security Advisories 私下报告漏洞，不要在公开 Issue 中提交令牌、密码、数据库转储或可识别的业务数据。

## 部署要求

- 仓库中的配置只作为开发模板。生产环境的数据库、Redis、JWT、对象存储等凭据必须通过环境变量或受控的密钥管理系统提供。
- 首次部署必须设置本地管理员密码，并按 `sql/README.md` 完成 SQL 初始化。
- JimuReport 数据源口令和分享链接必须在部署环境中重新创建，不得回填到公开 SQL。
- 公网部署前应关闭不需要的 Swagger、Druid、报表设计器和大屏设计能力，并在反向代理与应用权限层同时限制管理入口。
- 若任何凭据曾进入 Git 历史，应立即轮换；从当前文件删除并不能使旧提交中的凭据失效。

