# 供应链采购审批与票据台账系统

## V1.0 需求与系统详细设计

**文档日期：**2026-09-06  
**文档用途：**作为页面设计、数据库设计、接口开发和验收的共同基线。  
**交付范围：**仅设计，不包含程序开发、环境安装或部署。  
**来源：**用户提供的《供应链采购审批与票据台账系统.md》。原文件保持不变。  
**设计取向：**优先支撑可运行、可保存数据的完整演示版，技术栈允许简化。本文补充的业务规则属于设计默认方案，不代表已经获得业务负责人的逐项确认。

---

## 1. 系统目标与版本范围

系统围绕一条业务链工作：

**供应商准入 → 采购订单审批 → 分批入库 → 发票登记及两级审批 → 自动形成台账。**

每张发票可追溯到供应商、采购订单、订单明细、实际入库明细和历次审批。系统重点解决单据关联、退回修改、数量占用、金额口径和历史留痕，审批通过不表示已经付款。

### 1.1 本版必须包含

| 模块 | 交付能力 |
| --- | --- |
| 工作台 | 当前角色待办、本人申请、业务统计、最近处理记录 |
| 供应商 | 新建、附件、提交、主管审批、退回修改、准入、受限作废 |
| 采购订单 | 选择已准入供应商、多行商品、主管审批、版本历史 |
| 入库 | 选择已批准订单、分批收货、剩余数量校验、凭证附件 |
| 发票 | XML/PDF/OFD 附件、约定 XML 解析、人工确认、行级关联、重复检查 |
| 审批中心 | 待办、已办、申请、通过、退回、撤回、意见及完整流程记录 |
| 台账 | 供应商、采购、入库、发票、综合台账；筛选、追溯、Excel 导出 |
| 基础配置 | 公司信息、演示用户、角色、固定流程的候选审批人 |
| 审计 | 提交版本、字段变化、附件版本、审批意见、追加评论、作废与恢复记录 |

### 1.2 本版不包含

付款和应付核销、银行对接、ERP 同步、税务验真、OCR、所有厂商的发票 XML 兼容、红字发票、采购退货、跨订单合票、外币、复杂会签、自由绘制工作流、批准后直接修改原单。

已确认入库的冲销、更正和退货属于后续版本。本版应在确认入库前提供完整复核，避免把演示系统误当作已具备上述能力的生产系统。

### 1.3 完成定义

“可用”指本版范围内的业务闭环能够完整执行，数据能够持久保存，权限和校验由后端落实，失败后不产生半完成数据，并通过本文第 17 章验收。仅有页面跳转或浏览器临时数据不视为完成。

## 2. 原文问题与统一决策

原文包含需求、讨论和相互重复的修改建议。以下表格将尚未闭合的规则收敛为一个可实现的版本。

| 编号 | 需要统一的问题 | 本版设计决策 |
| --- | --- | --- |
| D01 | 状态与当前节点重复 | 审批状态和当前节点分字段保存；通过后当前节点为空 |
| D02 | 退回是否保留数量 | 退回、撤回、作废均释放占用；重新提交重新争取可用数量，不足则禁止提交 |
| D03 | 发票与入库关联层级 | 发票关联一张订单；发票行关联订单行；系统将开票数量分配到具体入库行 |
| D04 | 是否手选入库批次 | 默认按确认时间先后自动分配，用户可查看分配结果，本版不提供手动指定批次 |
| D05 | 发票唯一性 | 规范化后的“销方税号＋发票号码”；发票类型不参与唯一键 |
| D06 | 作废发票是否可重复登记 | 不允许另建同号记录；需要修正时，管理员恢复原记录，再完整重审 |
| D07 | 订单作废 | 草稿/退回按权限处理；已批准且没有已确认入库、没有有效下游引用时，主管或管理员可作废 |
| D08 | 已审核发票有误 | 财务业务作废 → 管理员恢复原记录 → 原提交人修改 → 采购与财务重新审批 |
| D09 | 审批中的业务修改 | 业务字段、明细和附件锁定；允许追加评论；修改须先退回或撤回 |
| D10 | 单价或税率差异 | 允许差异，差异说明必填；允许因价格差异使发票金额超过订单，由两级审批决定 |
| D11 | XML 能力边界 | 只支持本文约定的 DemoInvoice XML v1，其他结构保留原文件并提示人工录入 |
| D12 | 金额计算口径 | 订单按有效订单统计，正式发票按当前已通过且未作废统计，待审金额单列 |
| D13 | 找不到审批人 | 提交前检查；找不到合法审批人则整笔提交失败，不生成空待办 |
| D14 | 技术实现复杂度 | 本版推荐模块化单体＋内置固定工作流，同一数据库事务处理业务与审批 |
| D15 | 演示角色切换 | 仅用于隔离的本地演示；后端仍校验用户身份、角色、归属和审批任务 |

**D02 的明确后果：**A 发票退回后，B 发票可以使用已释放数量；A 重新提交时若余额不足，必须调整数量后再提交。系统应明确解释这一原因，不能静默挤占 B 的数量。

## 3. 角色、权限与数据范围

### 3.1 用户模型

本版一个用户对应一个业务角色，用户可启用或停用。保留稳定的用户 ID，审批历史不随用户改名而丢失；历史记录保存当时的姓名及角色快照。

角色切换应切换服务端会话中的演示用户，不允许通过修改前端显示名称、请求中的角色字段或按钮状态获得权限。正式多人使用时，应接入真实登录，关闭演示身份切换。

### 3.2 操作权限矩阵

“本人”指当前单据的创建人；“指定”指该流程实例在提交时确定的审批人。

| 操作 | 经办人 | 采购员 | 采购主管 | 仓管员 | 财务 | 管理员 |
| --- | --- | --- | --- | --- | --- | --- |
| 创建供应商、订单、发票 | 是 | 否 | 否 | 否 | 否 | 是 |
| 编辑、提交草稿或退回单据 | 本人 | 否 | 否 | 否 | 否 | 本人 |
| 删除从未提交的草稿 | 本人 | 否 | 否 | 否 | 否 | 本人 |
| 撤回审批中的申请 | 本人 | 否 | 否 | 否 | 否 | 本人 |
| 供应商、订单审批 | 否 | 否 | 指定 | 否 | 否 | 否 |
| 确认入库 | 否 | 否 | 否 | 是 | 否 | 是 |
| 发票采购审核 | 否 | 指定 | 否 | 否 | 否 | 否 |
| 发票财务审核 | 否 | 否 | 否 | 否 | 指定 | 否 |
| 作废本人草稿/退回单据 | 本人 | 否 | 否 | 否 | 否 | 本人 |
| 作废已批准供应商或订单 | 否 | 否 | 是，需校验引用 | 否 | 否 | 是，需校验引用 |
| 作废已通过发票 | 否 | 否 | 否 | 否 | 是，且非提交人 | 否 |
| 恢复已作废发票 | 否 | 否 | 否 | 否 | 否 | 是，需填写原因 |
| 查看台账 | 本人范围 | 分配范围 | 全局 | 仓储范围 | 全局 | 全局 |
| 导出 Excel | 否 | 否 | 全局 | 否 | 全局 | 全局 |
| 追加评论 | 对可见单据 | 对可见单据 | 对可见单据 | 对可见单据 | 对可见单据 | 对可见单据 |
| 用户、公司、流程候选人配置 | 否 | 否 | 否 | 否 | 否 | 是 |

管理员不自动拥有采购或财务审批权，也不能以“管理员”身份绕过业务校验。

### 3.3 数据范围

| 角色 | 可以读取的数据 |
| --- | --- |
| 经办人 | 本人创建的业务单据及其必要关联资料；供应商选择器可查看已准入供应商的名称、税号和准入状态 |
| 采购员 | 指派给自己的采购订单、相关入库、相关供应商及发票；本人待办和已办 |
| 采购主管 | 全部业务单据、关联资料和台账 |
| 仓管员 | 已批准订单及必要供应商资料、全部入库记录；不读取完整发票及发票原件 |
| 财务 | 全部业务单据和台账，用于对账及发票审核 |
| 管理员 | 全部业务数据、配置和审计记录 |

列表、详情、搜索建议、统计、导出、附件下载均适用相同的数据范围。无权限的记录不能通过猜测 ID 访问。选择器只返回完成当前选择所需的字段，不应顺带暴露银行账号和资质附件。

### 3.4 审批人分配

1. 供应商、订单：从该流程配置的主管候选人中，按配置顺序选择第一个启用且非提交人的用户。
2. 发票采购节点：使用关联订单上指定的采购员，必须启用且非提交人。
3. 发票财务节点：从财务候选人中按配置顺序选择首个合法用户，该用户不得等于提交人或采购审核人。
4. 每次提交时确定两级审批人并保存快照；后续配置变更不自动改变已经创建的待办。
5. 有待办或被运行中流程预留为后续审批人的用户，禁止直接停用或改角色。管理员可将已生成、尚未处理的任务转交给同角色合法用户，原因必填；已完成意见保持原作者。未生成的后续审批人需要更换时，先撤回申请，再按新配置提交。
6. 发票若在采购通过后更换财务处理人，新财务仍须与该版本提交人、实际采购审核人不同。

## 4. 状态机与操作规则

### 4.1 字段拆分

| 维度 | 字段 | 取值 |
| --- | --- | --- |
| 审批状态 | approval_status | DRAFT 草稿、IN_REVIEW 审批中、RETURNED 退回修改、APPROVED 已通过、VOID 已作废 |
| 当前节点 | current_node | SUPERVISOR 主管、PURCHASE 采购、FINANCE 财务、空 |
| 订单收货进度 | receipt_progress | NONE 未入库、PARTIAL 部分入库、FULL 全部入库；由明细计算 |
| 订单开票进度 | invoice_progress | NONE 未开票、PARTIAL 部分开票、FULL 全部开票；仅按已通过发票数量计算 |
| 入库业务状态 | receipt_status | CONFIRMED 已确认；入库无独立审批状态 |

审批中必须有当前节点；草稿、退回、已通过、已作废的当前节点为空。界面可组合显示“审批中 · 待财务审核”，但不能将该显示文本作为数据库状态值。

### 4.2 通用状态迁移

| 当前状态 | 操作 | 执行人 | 目标状态 | 附加处理 |
| --- | --- | --- | --- | --- |
| 草稿 | 保存 | 本人 | 草稿 | 不启动审批，不占用开票数量 |
| 草稿/退回 | 提交 | 本人 | 审批中 | 全量校验，生成新版本和流程实例 |
| 审批中 | 退回 | 当前指定审批人 | 退回修改 | 原因必填，结束当前流程，取消未开始节点，释放发票占用 |
| 审批中 | 撤回 | 本人 | 退回修改 | 原因必填，取消待办，释放发票占用，保留已完成意见 |
| 审批中 | 最终通过 | 当前指定审批人 | 已通过 | 当前节点置空，订单生效或发票进入正式统计 |
| 草稿/退回 | 作废 | 本人 | 已作废 | 原因必填，保留记录；无未结束任务 |
| 已通过 | 作废 | 按 4.4 节 | 已作废 | 校验下游引用；发票释放占用并移出当前正式金额 |
| 已作废发票 | 恢复原记录 | 管理员 | 退回修改 | 原因必填；不恢复旧审批、不占用数量、不直接进入正式台账 |

只有 `提交版本数=0` 的草稿允许删除。删除为软删除并记录审计；已经提交过的单据只能退回、撤回或作废，不提供删除入口。

### 4.3 三条固定流程

| 流程 | 提交后 | 中间通过 | 最终通过 | 任一节点退回后 |
| --- | --- | --- | --- | --- |
| 供应商准入 | 待主管审批 | 无 | 已准入 | 回到本人，重提交仍从主管开始 |
| 采购订单审批 | 待主管审批 | 无 | 订单生效 | 回到本人，重提交仍从主管开始 |
| 发票双审 | 待采购审核 | 采购通过 → 待财务审核 | 已通过 | 回到本人，重提交一律从采购开始 |

财务退回后的采购旧意见仅属于旧版本，不能被新版本复用。待审数量在采购通过、流转到财务时持续占用，不因节点转换短暂释放。

### 4.4 作废与恢复

| 单据 | 已通过后允许作废的条件 | 后续处理 |
| --- | --- | --- |
| 供应商 | 主管或管理员操作；没有未删除、未作废的引用订单，也没有已确认入库等历史有效下游业务 | 禁止再用于创建或提交订单；不提供恢复 |
| 订单 | 主管或管理员操作；没有任何已确认入库，且没有未删除、未作废的关联发票 | 保留历史，从有效订单金额中移除；不提供恢复 |
| 发票 | 财务操作，且不是该版本提交人；作废原因必填 | 释放开票数量、保留号码唯一性；必要时由管理员恢复原记录 |
| 入库 | 本版不支持确认后作废、删除或覆盖修改 | 更正和冲销能力列入后续版本，不允许通过改数据库替代 |

审批中不直接作废，先由本人撤回或审批人退回，再作废。已通过发票的“作废”是本系统的业务处理，不代表完成税务红冲或外部票据作废。

同一张发票恢复后仍使用原记录 ID 和系统单号，重新提交生成更高版本；已作废的历史事件、旧附件和旧审批仍可查看。

## 5. 单据字段与表单设计

### 5.1 供应商

| 字段 | 提交要求及规则 |
| --- | --- |
| 供应商名称 | 必填，2～200 字符，去除首尾空白 |
| 统一社会信用代码/销方税号 | 必填，本版供应商限定 18 位大写字母或数字；格式检查不等于真实性验证 |
| 联系人、联系电话 | 必填；电话允许国家/地区前缀和分机，不限于手机号 |
| 地址 | 必填，最多 500 字符 |
| 开户行、账户名称、银行账号 | 提交时必填；账号按字符串保存，保留前导零 |
| 资质附件 | 提交时至少一个 PDF/PNG/JPG 文件；同一供应商可有多个 |
| 备注 | 选填，最多 2,000 字符 |

供应商税号按规范化值查重，未删除记录含作废记录参与校验。已通过供应商字段锁定，本版不提供准入资料变更流程。

### 5.2 采购订单

表头：系统单号、供应商、指定采购员、预计到货日期、币种 CNY、备注、附件、创建人和时间。

明细至少一行，每行保存：稳定行 ID、行号、商品名称、规格型号、单位、数量、未税单价、税率、未税金额、税额、含税金额。

- 供应商在创建和提交时均须已准入；提交时重新读取其状态。
- 采购员必须是启用的采购角色用户。
- 商品名称、单位、数量、单价、税率必填；规格可选。
- 金额由系统计算，订单表头汇总不能手工覆盖。
- 仅允许正数数量、正数单价，税率可以为零。
- 无有效商品行时不能提交；同名商品允许出现多行，关联时使用行 ID，不用商品名称匹配。

### 5.3 入库单

表头：系统单号、订单、仓库、入库业务日期、确认人、确认时间、凭证附件、备注。

明细：订单行 ID、商品名称及单位快照、订单数量、此前已入库数量、剩余可入库数量、本次实收数量。

- 订单必须已通过且未作废。
- 单张入库单只关联一张订单，可选择部分订单行。
- 本次未收货的行可以填零，但保存时不产生零数量入库行。
- 至少有一行数量大于零；同一入库单中同一订单行只出现一次。
- 确认前显示完整复核页；点击确认后一次性写入单头、明细、审计和快照。
- 日期不得晚于公司业务时区的当天；允许记录实际发生在过去的入库日期。

### 5.4 发票

| 区域 | 字段 |
| --- | --- |
| 票据身份 | 系统单号、发票号码、发票类型、开票日期、币种 |
| 销方 | 名称、税号；必须与关联供应商匹配 |
| 购方 | 名称、税号；必须与公司配置匹配 |
| 关联业务 | 一张已通过且已发生入库的订单；按行关联订单明细 |
| 发票明细 | 原票商品名称、规格、单位、数量、未税单价、税率、未税金额、税额、对应订单行 |
| 金额汇总 | 未税金额、税额、价税合计、舍入差额 |
| 说明 | 单价/税率/单位差异说明、单据备注 |
| 文件与确认 | 原始附件、解析结果、解析状态、人工确认人/时间/内容摘要 |

发票号码按数字字符串保存，长度 1～32，保留前导零。类型仅用于分类，本版不以号码长度自动推断税务真实性。

保存草稿允许业务字段不完整，但非空字段仍须符合类型、长度和重复规则。提交时所有必填项必须完整，至少有一个有效原始票据附件，并完成针对当前内容的人工确认。

## 6. 数量、金额和占用规则

### 6.1 数值约定

| 数值 | 业务精度 | 推荐存储 |
| --- | --- | --- |
| 数量 | 最多 4 位小数 | 乘以 10,000 的整数，记为 Q4 |
| 未税单价 | 最多 6 位小数 | 乘以 1,000,000 的整数，记为 P6 |
| 税率 | 按比例最多 4 位小数，例如 0.1300 | 乘以 10,000 的整数，记为 R4 |
| 金额、税额、价税合计 | 2 位小数 | 人民币分的整数 |

接口使用十进制字符串传输数量、单价和金额。后端使用整数或十进制定点计算，不以二进制浮点数直接比较金额。

设计上限：单行数量和单价均小于 100,000,000，单据价税合计不超过 999,999,999,999.99 元。超出范围直接提示，不截断、不自动改精度。乘法中间值使用足够精度的整数，最终结果检查存储范围。

### 6.2 订单金额

- 行未税金额 = 数量 × 未税单价，四舍五入到分。
- 行税额 = 已舍入的行未税金额 × 税率，四舍五入到分。
- 行含税金额 = 行未税金额 + 行税额。
- 订单金额、税额、价税合计分别为各行对应金额之和。
- 舍入统一采用非负数 ROUND_HALF_UP；不采用前后端各自默认的不同舍入方式。

### 6.3 发票金额

1. 发票每行未税金额与“数量 × 未税单价”的舍入结果差异不得超过 0.01 元。
2. 每行税额与“行未税金额 × 税率”的舍入结果差异不得超过 0.01 元。
3. 表头未税金额必须等于行未税金额合计，表头税额必须等于行税额合计。
4. 表头价税合计与“未税金额＋税额”的差额只允许 −0.01、0、+0.01 元，保存为显式舍入差额，不静默修正原票金额。
5. 台账按确认后的票面价税合计统计；舍入差额可在详情中查看。
6. 发票数量、单价须为正，税额不得为负，价税合计须大于零。
7. 单价或税率与订单行不一致时必须填写差异说明；系统不以订单金额为硬性上限。
8. 发票单位须与订单单位一致，本版不支持跨单位换算。仅为同一单位的名称别称时，可人工映射为订单单位，保留原票单位和说明，不改变数量；需要实际换算的发票不在本版范围内。

### 6.4 入库上限

对每一个订单行计算：

> 剩余可入库数量 = 订单行数量 − 所有已确认入库行数量合计。

本次实收数量不得超过剩余值。跨行、跨请求不能互相抵消超量；校验和入库写入必须在同一事务中完成。

### 6.5 可开票数量

对每一个入库行计算：

> 可开票数量 = 已确认入库数量 − 审批中保留数量 − 已通过确认数量。

草稿、退回、撤回、已作废发票不占用数量。对订单行的可开票数量，为其全部入库行可用数量之和。

提交时对同一订单行的多条发票行先合计需求，再校验总余额；不能逐行使用同一个旧余额，导致重复分配。

### 6.6 自动分配算法

1. 校验所有发票行均属于当前关联订单。
2. 读取对应订单行的已确认入库行，按 `确认时间 → 入库单号 → 入库行号` 排序。
3. 减去每个入库行仍有效的审批中/已通过分配，取得可用余额。
4. 按发票行号顺序，优先使用最早批次，可跨批次拆分。
5. 所有发票行均能分配后，才写入分配、提交快照、流程实例和任务。
6. 任何一行数量不足，整笔事务回滚，不保留部分分配或部分待办。

示例：订单 A 商品 100 个，先后入库 60 个和 40 个；已有有效开票 30 个占用第一批。新发票开 50 个时，分配第一批剩余 30 个、第二批 20 个。该发票退回后，这两笔分配同时释放。

### 6.7 发票号码唯一性

- 税号：去除首尾空格并转大写；内部空格不自动删除，应提示修正。
- 发票号码：去除首尾空格，保持字符串及前导零。
- 唯一键：`normalized_seller_tax_id + normalized_invoice_number`。
- 草稿、审批中、退回、已通过、已作废记录均参与当前身份校验。
- 已经提交过的历史身份永久归属原发票 ID。号码或税号更正时新增身份保留记录，原身份不释放，避免历史票据被另建重复记录。
- 从未提交的草稿被删除或修正身份时，其未曾提交过的旧身份可以释放。
- 同一原发票 ID 可继续使用自己的保留身份；其他记录冲突时返回现有单号，且仅在有权查看时提供跳转链接。

## 7. 附件与 XML 解析

### 7.1 上传、访问与锁定

| 单据 | 允许格式 | 限制 |
| --- | --- | --- |
| 发票 | XML、PDF、OFD | 单文件 ≤20 MB，单据最多 10 个当前附件 |
| 供应商、订单、入库 | PDF、PNG、JPG/JPEG | 单文件 ≤20 MB，单据最多 10 个当前附件 |

后端检查大小、扩展名、类型及基础文件签名；不信任浏览器的 MIME 声明。文件用服务端生成的 ID 存储，原文件名只作为显示字段，不能拼接存储路径。

PDF 提供预览和下载；OFD 提供下载；XML 提供下载和转义后的文本查看。预览不能直接执行上传文件中的脚本或 HTML。每次附件访问校验所属单据的数据权限，不提供公开直链。

审批中和已通过禁止新增、替换或解绑业务附件。草稿/退回可以调整当前附件集合，但已进入历史提交版本的文件不得物理覆盖或删除。

### 7.2 约定 XML v1

本版只解析根节点为 `DemoInvoice`、属性 `schemaVersion="1.0"` 的 UTF-8 文件。此结构是项目演示约定，不是对国家或厂商发票 XML 标准的声明。

| XML 路径 | 目标字段 | 要求 |
| --- | --- | --- |
| Header/InvoiceNumber | 发票号码 | 必填 |
| Header/InvoiceType | 发票类型 | 必填 |
| Header/IssueDate | 开票日期 | 必填，YYYY-MM-DD |
| Header/Currency | 币种 | 必填，固定 CNY |
| Seller/Name、Seller/TaxId | 销方名称、税号 | 必填 |
| Buyer/Name、Buyer/TaxId | 购方名称、税号 | 必填 |
| Lines/Line/Name、Specification、Unit | 商品、规格、单位 | 商品及单位必填 |
| Lines/Line/Quantity、UnitPrice、TaxRate | 数量、未税单价、税率 | 必填；税率为比例，如 0.13 |
| Lines/Line/Amount、TaxAmount | 行未税金额、行税额 | 必填 |
| Totals/Amount、TaxAmount、TotalAmount | 未税合计、税额、价税合计 | 必填 |

XML 不携带可信订单 ID、审批人、审批状态或数量分配。订单和订单行关联由操作人选择并经后端校验，不能由上传文件控制。

### 7.3 解析与人工确认

1. 用户先创建或打开发票草稿，再上传文件；上传不自动提交。
2. 原文件持久保存后解析，保存解析器版本、原始解析结果和解析状态。
3. 新草稿可将成功解析字段回填到空白表单；已有字段不静默覆盖，应展示差异供用户采用。
4. 用户补充关联订单和明细映射，核对税号、数量、单价、金额及附件。
5. 点击“确认票面信息”后，记录确认人、时间和被确认内容的摘要。
6. 任何业务字段、明细或当前附件集合变化都会使人工确认失效；再次确认后方可提交。
7. 解析失败保留原文件，可人工录入并确认；界面不显示“税务验真通过”。

### 7.4 解析失败分类

`UNSUPPORTED_SCHEMA` 结构不支持；`MALFORMED_XML` 格式错误；`ENCODING_UNSUPPORTED` 编码不支持；`MISSING_FIELD` 缺字段；`INVALID_NUMBER` 数值错误；`UNSAFE_XML` 含禁止的 DTD/外部实体等内容；`LIMIT_EXCEEDED` 超出大小、深度或节点数限制。

建议最大层级 12、明细行 500；禁止 DTD、外部实体和外部资源请求。错误提示说明字段或原因，允许继续人工录入，不得因解析失败丢弃原始文件。

## 8. 版本、意见与审计

### 8.1 三个不同编号

| 标识 | 用途 | 何时变化 |
| --- | --- | --- |
| 单据 ID/系统单号 | 标识同一笔业务 | 创建后不变，作废恢复也不变 |
| 提交版本号 version_no | 标识审批针对哪一版内容 | 每次成功提交加 1；保存草稿不增加 |
| 并发修订号 revision | 防止旧页面覆盖新数据 | 保存业务字段、改附件、提交、审批、撤回、作废、恢复时递增 |

供应商、订单和发票均保留提交快照。入库虽然不走审批，确认时也保存一次不可覆盖的业务快照。

### 8.2 提交快照内容

单头、所有明细、金额、必要主数据名称与税号快照、附件 ID 与文件摘要、人工确认记录、发票入库分配明细、提交人、提交时间、候选人配置版本、实际指定审批人。

历史页面以快照为准，不能通过读取当前供应商名称、当前附件集合拼出一个已经被修改的“历史版本”。

### 8.3 修改记录

保存草稿和退回单据时记录字段路径、修改前值、修改后值、修改人和时间。明细以稳定行 ID 比对，不以行号判断是不是同一行。附件新增/移除记录文件 ID、名称及摘要，不把文件内容写入日志。

人工修改解析字段时，同时保留原始解析结果及修改记录。被修改的解析结果不能覆盖原 XML 内容或旧版本。

### 8.4 三类文字记录

| 类型 | 是否随业务版本保存 | 是否可以覆盖历史 |
| --- | --- | --- |
| 单据备注 | 是；草稿/退回时允许修改当前值 | 不能覆盖旧版本值 |
| 审批意见 | 关联流程、任务、版本、审批人 | 提交后不允许修改或删除 |
| 追加评论 | 追加到单据时间线，包含作者与时间 | 不允许修改或删除 |

评论不改变业务内容，不使人工确认失效，不改变业务并发修订号。审批意见建议不超过 2,000 字；退回、撤回、作废、恢复和转交原因去除空白后至少一个字符。

## 9. 页面与交互设计

### 9.1 信息架构与通用布局

一级导航：**工作台、供应商、采购订单、入库管理、发票管理、审批中心、台账中心、系统设置**。系统设置仅管理员可见。

桌面采用左侧导航、顶部角色与用户信息、中间工作区域。建议以 1440 像素宽为设计基准，侧栏约 216 像素，顶栏约 64 像素，工作区边距 24 像素。复杂订单/发票编辑使用完整页面，避免把商品表格塞进狭窄弹窗。

设计基调为清晰、紧凑的中文业务后台：白色内容区、浅灰背景、深蓝主操作。状态同时提供文字和颜色；金额右对齐，单号和金额采用易对齐数字。常规表格正文不小于 14 像素，正文建议 16 像素。小屏可折叠导航、横向滚动商品表格，不隐藏关键金额或审批结果。

通用交互规则：

- 列表默认按最近更新时间倒序，分页默认 20 条，支持 20/50/100 条。
- 搜索、日期和状态条件显示在列表顶部；提供“查询”和“重置”。
- 无数据时区分“尚无单据”和“没有符合条件的数据”。
- 按钮根据角色、归属和状态展示；禁用时说明原因，后端仍再次校验。
- 保存失败保留当前输入；有未保存内容离开页面时提示。
- 有关数量的结果以服务端返回值为准；显示“数据已变化，请刷新”时不能自动覆盖用户输入。
- 破坏性操作显示单号、影响和原因输入；审批通过也应显示此次版本及金额摘要。

### 9.2 工作台

| 区域 | 内容与操作 |
| --- | --- |
| 顶部 | 当前日期、当前用户与角色；演示环境明确标记“角色演示” |
| 待办区 | 本人待处理数量、最近待办、进入处理入口 |
| 统计区 | 当前可见范围内有效订单金额、已通过发票金额、待审发票金额；不混作同一个总额 |
| 本人申请 | 草稿、退回修改、审批中，优先显示需本人处理的退回单据 |
| 最近活动 | 提交、通过、退回、入库、作废等时间线，遵守数据范围 |

经办人快捷入口为“新建供应商 / 新建订单 / 登记发票”；仓管入口为“登记入库”；审核角色优先展示待办。不得向无权限角色显示可以执行的创建按钮。

### 9.3 供应商页面

列表列：单号、名称、信用代码、联系人、创建人、审批状态、更新时间、操作。筛选：名称/代码/单号、状态、创建日期。

详情按“基本信息、开户信息、资质附件、审批与版本”分区。草稿/退回时提供编辑、保存、提交；审批中显示当前处理人和流程；已准入详情可进入其订单列表。

新建时税号失焦可做预检查；最终保存仍由数据库唯一约束兜底。发现重复时不能自动覆盖原供应商。

### 9.4 采购订单页面

列表列：单号、供应商、采购员、预计到货、价税合计、审批状态、入库进度、开票进度、操作。

编辑页上半部填写表头，下半部编辑商品行。支持增行、删行、显示逐行金额；底部固定显示未税金额、税额、合计及“保存草稿 / 提交审批”。

订单详情设四个页签：订单明细、入库记录、关联发票、审批与版本。仓管员不展示完整发票页签；可按权限显示必要的仓储信息。

### 9.5 入库页面

操作顺序：选择订单 → 选择仓库及日期 → 填本次数量 → 复核 → 确认入库。

商品表必须并排显示订单数量、已收数量、剩余数量、本次实收。点击“按剩余数量填入”可以辅助录入，但确认时后端重新计算余额。

复核页明确显示“确认后本版不支持修改或冲销”，并列出本次正数量行和附件。确认失败时指出具体超量行，例如：“商品 A 当前最多可入库 20 个，本次填写 25 个”。

### 9.6 发票页面

新建/编辑页采用四个步骤，但可返回上一步：

| 步骤 | 核心内容 | 进入下一步的条件 |
| --- | --- | --- |
| 1 上传与录入 | 上传 XML/PDF/OFD、显示解析状态和票面字段 | 至少保留草稿；解析失败可手工继续 |
| 2 关联业务 | 选择订单，逐行映射订单商品，显示已收/占用/可开数量 | 每条发票行完成映射 |
| 3 核对差异 | 税号比对、金额核对、单价税率差异、附件预览、差异说明 | 必填项和本地预检查通过 |
| 4 确认与提交 | 当前版汇总、人工确认、预计审批人 | 服务端提交校验成功 |

页面上的“可开票数量”是查询时余额，不能保证提交一定成功。若被其他发票占用，应显示更新后的余额并保留原输入。

发票详情设“票面信息、业务关联与分配、原始附件、审批与版本”四个页签。业务关联页应能展开到具体入库单及入库行。

列表列：系统单号、发票号码、销方、关联订单、开票日期、价税合计、审批状态、当前节点、提交人、操作。相同发票补充其他格式时，应打开原记录追加附件，不重复新建。

### 9.7 审批中心

页签：我的待办、我的已办、我的申请。待办列：单据类型、单号、摘要、金额、版本、提交人、提交时间、当前节点。

审批详情优先展示当前提交快照；采购审核看订单、数量、价格和入库分配，财务审核看票面身份、税号、金额、原件和采购意见。

底部操作区：审批意见、“退回修改”、“通过”。退回原因必填。处理期间禁止重复点击。任务被别人处理或转交后，旧页面操作应提示“该任务已变化，请刷新”，不能默认为通过。

我的申请中提供撤回入口，但仅限本人且当前仍在审批中。我的已办可查旧版本及当时意见，不只展示当前业务状态。

### 9.8 台账中心

页签：综合台账、供应商台账、采购台账、入库台账、发票台账。

综合台账以订单为一行，展开显示订单明细、分批入库、关联发票及各自状态。支持供应商、单号、日期、采购员、审批状态筛选。字段定义和统计公式见第 14 章。

“导出 Excel”使用当前筛选和当前权限范围，导出前说明日期口径及记录数量；禁止仅导出当前屏幕可见的 20 行而不提示。

### 9.9 系统设置

- 公司：名称、税号、业务时区、默认币种。
- 用户：姓名、角色、启用状态；不得删除被历史引用的用户。
- 仓库：名称、启用状态；停用只影响新入库选择，不影响历史。
- 固定流程：主管候选列表、财务候选列表及其顺序。发票采购人仍来自订单。
- 任务转交：选择待办、同角色合法接收人、原因，显示转交日志。

流程配置不提供任意节点增删，不允许管理员改出跳过采购的发票路线。

## 10. 技术架构设计

### 10.1 推荐实现

| 层次 | 设计选择 | 原因 |
| --- | --- | --- |
| 前端 | Vue 3 + TypeScript + Element Plus | 延续原文的后台表单方向，便于实现中文表格及编辑页 |
| 后端 | Node.js + TypeScript，采用 NestJS 模块化单体 | 将权限、校验和事务集中在服务端，业务模块仍清晰隔离 |
| 工作流 | 应用内固定流程模块 | 与业务共享事务，避免演示版先处理跨服务双写 |
| 数据库 | SQLite，启用外键、事务和适合本地写入的日志模式 | 本地可持久化，不依赖单独部署数据库服务 |
| 文件 | 本地不可变文件目录，数据库保存元数据及版本引用 | 可按单据鉴权和追溯原件 |
| 交付运行形态 | 一个本地服务同时提供静态页面和 API | 降低试用环境配置成本 |

技术选型是设计建议，本次不安装依赖、不生成工程，也不声称已经完成实现。

### 10.2 模块边界

| 模块 | 职责 |
| --- | --- |
| Identity | 会话、用户、角色、数据范围 |
| MasterData | 公司、仓库、供应商准入资料 |
| Purchasing | 订单和商品行、收货及开票进度 |
| Receiving | 分批入库、超量校验 |
| Invoicing | 票据身份、解析、金额、关联和分配 |
| Workflow | 提交版本、流程实例、任务、审批、退回、撤回 |
| Attachments | 上传校验、不可变存储、鉴权下载和版本引用 |
| Ledger | 按统一口径查询、汇总及导出 |
| Audit | 业务动作、字段变化、评论和审批意见 |

控制器不直接修改审批状态。业务服务调用统一的状态迁移和权限校验；台账只读取业务事实及分配记录，不另做一套手工录入的数据源。

### 10.3 为什么本版不拆 Java + WarmFlow

原文的 Node、Java、工作流数据库之间需要处理提交失败、回调幂等和状态补偿。本版流程固定，先用同库事务满足一致性，更贴合“优先可用、技术可简化”的选择。

工作流模块保留清晰的内部服务边界，但不提前编写一个没有使用的远程工作流系统。未来若确需 WarmFlow，应作为独立迭代引入，并同步设计：

- 事务发件箱记录待发命令；本地提交不能先伪装为已经启动的审批。
- 工作流实例 ID、外部任务 ID 与本地业务版本一一关联。
- 回调按外部事件/任务 ID 幂等，失败可重试；过期版本回调不得更新新版本。
- “启动中/同步异常”与正式“审批中”有明确边界及补偿入口。
- 迁移期间禁止同一单据同时由内置流程和外部流程驱动。

这些是升级约束，不计入当前演示版的开发范围。

## 11. 数据模型

### 11.1 总体关系

一名用户可创建多张业务单据；一个已准入供应商对应多张订单；一张订单有多条订单明细和多张入库单；每条入库明细只属于一条订单明细。

一张发票只对应一张订单，但可有多条发票明细。每条发票明细关联一条订单明细，可通过多条分配记录覆盖多个入库批次。不同发票明细可以指向同一订单行，提交时按该行汇总校验。

每次提交生成一个单据版本和一个流程实例。流程实例包含按序处理的任务。评论、附件、审计与业务单据关联；审批意见与版本及任务关联。

### 11.2 标识及公共字段

内部主键使用不可预测的字符串 ID。显示单号按类型和业务日期生成，例如 `SUP-20260906-00001`、`PO-20260906-00001`、`GR-20260906-00001`、`INV-20260906-00001`。每日序号分类型递增，在数据库事务内分配并受唯一约束保护，不能使用“查询当前最大值＋1”。

服务端时间统一保存 UTC，页面按公司业务时区显示；业务日期单独以日期字段保存。历史审计时间不能由客户端任意填写。

### 11.3 主数据和业务表

下表列出开发必须落实的字段；所有外键均应建立索引，业务引用默认禁止级联删除。

| 表 | 核心字段 | 关键约束 |
| --- | --- | --- |
| company_profile | id、name、tax_id、timezone、currency、revision | 单公司；currency=CNY；存在已提交发票后名称/税号不可直接改 |
| users | id、name、role、enabled、created_at | 角色限定六种；历史用户不可物理删除 |
| warehouses | id、name、enabled | 停用不影响历史入库 |
| number_sequences | document_type、business_date、next_value | 类型＋日期唯一；事务递增 |
| documents | id、type、number、creator_id、creation_key、approval_status、current_node、current_version、revision、created_at、updated_at、deleted_at | number 唯一；creator_id＋creation_key 永久唯一；状态与节点组合检查；仅未提交草稿允许软删除 |
| suppliers | document_id、name、tax_id、dedup_key、contact、phone、address、bank_name、account_name、bank_account、remark | document_id 为主键和外键；dedup_key 唯一，存规范化税号；仅未提交草稿软删除时置空 |
| purchase_orders | document_id、supplier_id、buyer_id、expected_date、currency、amount_cents、tax_cents、total_cents、remark | 供应商、采购员均为外键；币种固定 |
| purchase_order_lines | id、order_id、line_no、is_current、name、specification、unit、quantity_q4、price_p6、rate_r4、amount_cents、tax_cents、total_cents | 当前行的 order_id＋line_no 唯一；非空数量/单价>0 |
| receipts | document_id、order_id、warehouse_id、business_date、confirmed_by、confirmed_at、remark | 一单一订单；确认后不可变 |
| receipt_lines | id、receipt_id、order_line_id、quantity_q4、item_snapshot | receipt_id＋order_line_id 唯一；数量>0 |
| invoices | document_id、order_id、invoice_number、type、issue_date、seller_name、seller_tax_id、buyer_name、buyer_tax_id、amount_cents、tax_cents、total_cents、rounding_delta_cents、difference_note、remark、confirmed_by、confirmed_at、confirmed_content_hash | 舍入差额只允许 -1/0/1 分；完整身份受保留表约束 |
| invoice_lines | id、invoice_id、line_no、is_current、order_line_id、name、specification、unit、original_unit、unit_alias_note、quantity_q4、price_p6、rate_r4、amount_cents、tax_cents | 当前行的 invoice_id＋line_no 唯一；订单行必须属于发票订单 |
| invoice_identities | id、seller_tax_id、invoice_number、invoice_id、ever_submitted、created_at | seller_tax_id＋invoice_number 唯一；已提交身份不释放 |
| invoice_allocations | id、invoice_id、version_id、invoice_line_id、receipt_line_id、quantity_q4、state、released_at、release_reason | state=RESERVED/CONFIRMED/RELEASED；数量>0；版本行＋入库行唯一 |

入库单仍使用 documents 提供公共 ID、单号和附件关联，但其 `approval_status` 与 `current_node` 均为空，确认状态由 receipts 表表示，不伪造一个入库审批流程。

草稿允许未完成的业务字段为空，存在值时执行类型和范围约束，提交服务执行完整性校验。移除订单行和发票行时标记 `is_current=false`，不物理删除被历史引用的行；当前行号使用带条件的唯一索引。当前表单和计算仅读取当前行，历史读取版本快照。

数量“不能超过某行剩余量”涉及跨行汇总，不能仅依赖普通字段 CHECK；必须在事务中完成余额检查和写入。

### 11.4 工作流、附件与审计表

| 表 | 核心字段 | 关键约束 |
| --- | --- | --- |
| workflow_configs | id、flow_type、version、supervisor_candidates、finance_candidates、updated_by、updated_at | 三条固定流程；配置更新生成新版本，候选顺序明确 |
| document_versions | id、document_id、version_no、snapshot_json、content_hash、submitted_by、submitted_at | document_id＋version_no 唯一；写入后不可覆盖 |
| workflow_instances | id、document_id、version_id、config_version、status、started_at、finished_at | 每个提交版本最多一个实例；status=RUNNING/APPROVED/RETURNED/WITHDRAWN |
| workflow_tasks | id、instance_id、node、sequence_no、assignee_id、status、decision、comment、acted_by、acted_at、revision | status=PENDING/COMPLETED/CANCELLED；实际办理人必须等于当时处理人 |
| attachment_files | id、storage_key、original_name、media_type、size_bytes、sha256、uploaded_by、uploaded_at、storage_status | storage_key 唯一；文件不可覆盖；单文件≤20 MB |
| document_attachments | document_id、attachment_id、is_current、created_at、removed_at | 文档＋文件唯一；移除当前引用不删除历史文件 |
| version_attachments | version_id、attachment_id、name_snapshot、sha256 | 版本＋文件唯一；固定版本附件集合 |
| parse_attempts | id、invoice_id、attachment_id、parser_version、status、raw_result_json、error_code、error_message、created_at | 每次解析独立保存，不覆盖旧尝试 |
| comments | id、document_id、author_id、author_snapshot、content、created_at | 仅追加 |
| audit_events | id、document_id、version_id、actor_id、actor_snapshot、action、reason、changes_json、request_id、created_at | 仅追加；可追溯转交、修改、释放和恢复 |
| idempotency_records | actor_id、request_key、route、payload_hash、response_status、response_json、created_at、expires_at | actor_id＋request_key 唯一；同键不同内容返回冲突 |
| sessions | id_hash、user_id、csrf_token_hash、expires_at、created_at | 不保存明文会话凭证；到期拒绝访问 |

任务按实际流转逐步创建：提交发票创建采购待办，采购通过后创建财务待办。财务审批人已在提交快照中确定。流程结束后没有待处理任务。

### 11.5 必须维护的不变量

1. 每条业务明细和每份附件都能追溯到合法父单据。
2. 发票行、分配涉及的入库行和订单行均属于同一发票关联订单。
3. 每个入库行的有效分配数量之和不超过其已确认数量。
4. 一份发票提交版本的有效分配之和，按发票行等于该版本开票数量。
5. 当前发票为审批中时，其当前版本分配是 RESERVED；已通过时为 CONFIRMED；退回/作废时没有有效分配。
6. 除入库外，当前单据审批中时恰有一个运行中实例和一个待处理任务。
7. 当前单据非审批中时，没有待处理任务；过去已完成流程保持原结果，不随业务作废被改写。
8. 审批必须针对实例绑定的版本；旧任务不得办理新版本。
9. 历史分配行及数量不可覆盖；状态可以从保留变确认或释放，并追加审计。
10. 统计读取当前有效事实；历史版本查看读取快照，两者不得互相代替。

### 11.6 查询索引

除唯一键和外键索引外，至少建立：

- documents：`type + approval_status + updated_at`、`creator_id + updated_at`。
- purchase_orders：`supplier_id`、`buyer_id`、`expected_date`。
- receipts：`order_id + confirmed_at`、`business_date`。
- invoices：`order_id`、`seller_tax_id + issue_date`、`issue_date`。
- workflow_tasks：`assignee_id + status + id`。
- invoice_allocations：`receipt_line_id + state`、`invoice_id + version_id + state`。
- audit_events：`document_id + created_at`。

## 12. 接口设计

### 12.1 通用约定

API 前缀 `/api/v1`；JSON 使用 UTF-8。成功返回 `data`，列表另含 `page、pageSize、total`。失败返回 `error.code、error.message、error.fields、requestId`。不把 SQL、内部路径或堆栈直接返回前端。

所有修改类请求携带 `Idempotency-Key`；修改现有单据业务内容或状态还携带 `If-Match` 对应 revision。追加评论不改变业务 revision，无需业务 `If-Match`；独立配置使用自身 revision；任务操作同时携带任务与所属单据修订号。前端每次新的操作生成新幂等键；因网络问题重试同一次操作使用原键，不再次生成。

客户端只提交允许编辑的字段。创建人、系统单号、审批状态、金额权威计算结果、审批人身份和审计时间由服务端产生或校验，不允许通过通用更新接口注入。

### 12.2 接口清单

| 方法与路径 | 用途 | 主要限制 |
| --- | --- | --- |
| GET /session | 当前用户、角色、环境、权限摘要 | 读取服务端会话 |
| POST /demo-session | 切换演示用户 | 仅显式启用的本地演示模式；校验同源 |
| DELETE /session | 退出当前会话 | 清除会话及浏览器凭证 |
| GET /dashboard | 工作台统计和待办摘要 | 自动按用户数据范围查询 |
| GET /suppliers | 供应商列表/选择器 | 选择器与完整列表分字段授权 |
| POST /suppliers | 创建供应商草稿 | 经办人/管理员 |
| GET /suppliers/{id} | 供应商详情 | 记录级授权 |
| PATCH /suppliers/{id} | 保存当前供应商 | 本人，草稿/退回 |
| GET /orders | 订单列表/选择器 | 选择发票订单时只返回合法候选 |
| POST /orders | 创建订单草稿 | 经办人/管理员，供应商已准入 |
| GET /orders/{id} | 订单详情及汇总 | 关联数据按权限裁剪 |
| PATCH /orders/{id} | 保存订单及明细 | 本人，草稿/退回；整单原子保存 |
| GET /orders/{id}/availability | 逐行可入库/可开票数量 | 查询结果仅供展示，不替代提交校验 |
| GET /receipts | 入库列表 | 按数据范围 |
| POST /receipts | 确认入库 | 仓管/管理员；无另一个“直接通过”接口 |
| GET /receipts/{id} | 入库详情 | 记录级授权 |
| GET /invoices | 发票列表 | 按数据范围 |
| POST /invoices | 创建发票草稿 | 经办人/管理员 |
| GET /invoices/{id} | 发票详情及当前分配 | 记录级授权 |
| PATCH /invoices/{id} | 保存票面字段和行映射 | 本人，草稿/退回；使人工确认失效 |
| POST /invoices/{id}/confirm | 确认当前票面信息 | 绑定当前内容摘要及 revision |
| POST /invoices/{id}/restore | 恢复已作废原记录 | 管理员，原因必填；回到退回修改 |
| POST /documents/{id}/submit | 提交审批 | 本人，草稿/退回，生成新版本 |
| POST /documents/{id}/withdraw | 撤回审批 | 本人，审批中，原因必填 |
| POST /documents/{id}/void | 业务作废 | 按类型/状态/角色及引用检查 |
| DELETE /documents/{id} | 删除未提交草稿 | 本人，version_no=0；软删除 |
| GET /documents/{id}/versions | 历次提交列表 | 记录级授权 |
| GET /documents/{id}/versions/{version} | 某版快照及审批 | 不返回当前内容冒充历史 |
| GET /documents/{id}/timeline | 审批、评论、变化时间线 | 记录级授权 |
| POST /documents/{id}/comments | 追加评论 | 对单据有读取权，内容非空 |
| POST /documents/{id}/attachments | 上传并关联文件 | 本人且可编辑；入库文件使用预上传后确认绑定 |
| DELETE /documents/{id}/attachments/{fileId} | 移除当前附件关联 | 可编辑状态；不删除版本文件 |
| GET /documents/{id}/attachments/{fileId} | 预览/下载原件 | 文件确属当前或历史版本，且用户有权限 |
| POST /invoices/{id}/parse | 解析指定 XML 附件 | 本人，可编辑状态；保留解析尝试 |
| POST /invoices/{id}/apply-parse | 采用选定解析字段 | 明确字段清单，记录前后值，不改订单和状态 |
| POST /uploads | 为尚未确认的入库预上传文件 | 仓管/管理员；临时所有者绑定当前用户 |
| GET /tasks | 我的待办/已办 | 不接受任意用户 ID 越权查询 |
| POST /tasks/{id}/approve | 通过当前任务 | 当前处理人，任务及单据修订号有效 |
| POST /tasks/{id}/return | 退回当前任务 | 当前处理人，原因必填 |
| POST /tasks/{id}/reassign | 转交未处理任务 | 管理员，同角色且通过职责分离校验 |
| GET /ledger/{type} | 台账查询 | type 在五类台账枚举中 |
| GET /ledger/{type}/export | 导出当前筛选 | 主管/财务/管理员，服务端查询完整范围 |
| GET/PATCH /settings/company | 公司配置 | 修改仅管理员，税务身份锁定规则生效 |
| GET/POST/PATCH /settings/users | 用户查询/新增/更新 | 修改仅管理员；更新具体用户使用 /{id} |
| GET/POST/PATCH /settings/warehouses | 仓库维护 | 修改具体仓库使用 /{id}；历史不删除 |
| GET/PATCH /settings/workflows | 固定流程配置 | 仅管理员，不允许改变审批顺序 |

入库预上传返回临时文件 ID；确认入库请求附带这些 ID，同一事务将文件元数据绑定到新入库单。临时文件只允许上传者使用，不因知道文件 ID 就能引用他人附件。

### 12.3 关键错误码

| HTTP | 业务错误码 | 前端处理 |
| --- | --- | --- |
| 401 | UNAUTHENTICATED | 提示会话失效，回到演示身份选择/登录 |
| 403 | FORBIDDEN | 提示没有操作权限，不提供绕过入口 |
| 404 | NOT_FOUND | 记录不存在或不可见 |
| 409 | REVISION_CONFLICT | 提示数据已更新，允许刷新并保留未保存输入供比较 |
| 409 | DUPLICATE_INVOICE / DUPLICATE_SUPPLIER | 显示重复原因；有读取权才提供原单链接 |
| 409 | RECEIPT_QUANTITY_EXCEEDED | 显示超量订单行和当前余额 |
| 409 | INVOICE_QUANTITY_EXCEEDED | 显示不足的订单行和可开余额 |
| 409 | TASK_ALREADY_HANDLED | 刷新任务和流程，不重复写意见 |
| 409 | IDEMPOTENCY_KEY_REUSED | 同键不同请求内容；前端不得默默重放不同业务 |
| 409 | DOCUMENT_REFERENCED | 作废受下游业务阻止；列出有权查看的引用 |
| 422 | NO_ELIGIBLE_APPROVER | 指出缺少主管、采购或财务，保留当前草稿 |
| 422 | INVALID_STATE / SELF_APPROVAL_FORBIDDEN | 解释当前状态或职责分离限制 |
| 422 | TAX_ID_MISMATCH / AMOUNT_MISMATCH | 定位到具体税号或金额字段 |
| 422 | MANUAL_CONFIRMATION_REQUIRED | 跳转票面确认区域 |
| 413 | FILE_TOO_LARGE | 显示 20 MB 上限 |
| 415 | UNSUPPORTED_FILE_TYPE | 显示当前单据允许的格式 |
| 503 | DATABASE_BUSY | 保留输入，可使用同一幂等键重试 |

## 13. 事务、并发与失败处理

### 13.1 提交发票的事务边界

下列操作必须全部成功或全部失败：

1. 认证用户，开启写事务，检查幂等记录。
2. 读取当前单据，校验本人、状态、revision 和人工确认内容摘要。
3. 校验发票身份、购销方、金额、订单状态、所有行映射及合法审批人。
4. 从数据库重新计算逐个入库行可用数量，完成全部分配计划。
5. 分配新版本号，写入不可变表单、附件和分配快照。
6. 写入 RESERVED 分配，保留该发票用过的身份。
7. 创建运行中流程实例及采购待办。
8. 将单据更新为 IN_REVIEW/PURCHASE，递增 revision。
9. 追加审计，保存幂等响应，提交事务。

任何步骤失败都回滚上述业务变化。不能出现“单据显示审批中，但没有流程或没有数量分配”的情况。

### 13.2 审批、退回和撤回

采购通过：事务内校验任务与版本 → 完成采购任务及意见 → 创建财务任务 → 当前节点改为 FINANCE。数量分配保持 RESERVED。

财务通过：事务内完成任务及流程 → 分配改为 CONFIRMED → 发票状态 APPROVED、节点为空 → 写审计和幂等响应。

退回/撤回：事务内完成当前任务或取消待办 → 结束实例 → RESERVED 分配改为 RELEASED → 单据 RETURNED、节点为空 → 写原因与审计。

已通过发票作废：事务内检查财务权限 → CONFIRMED 分配改为 RELEASED → 单据 VOID → 写作废原因。过去流程仍保持它当时的 APPROVED 结果，不篡改历史。

恢复：只改原单当前状态为 RETURNED 并记录恢复动作，既不恢复旧分配，也不创建审批任务。

### 13.3 入库及同一余额竞争

本地 SQLite 使用串行写事务，例如 BEGIN IMMEDIATE，覆盖“读取余额 → 校验 → 写入”全过程。不同用户即使同时打开旧余额，也只有取得事务后重新校验成功的请求能够写入。

后续迁移至支持行级锁的数据库时，应锁定相应订单行/入库行，按固定 ID 顺序加锁；继续保留相同的不变量，不能仅把数据库文件替换成远程连接。

所有同一订单的本次收货行一次性提交，任一超量则全部失败。数据库忙时返回可重试响应，不允许绕过事务保存部分行。

### 13.4 重复操作与过期页面

- 幂等键在同一用户范围内唯一；服务端对路由及规范化请求体计算摘要。
- 已成功操作的同键同内容请求返回原结果；同键不同内容返回冲突。
- 幂等记录与业务结果在同一事务提交。进程在提交后、返回前中断，重试仍能取得原结果。
- 幂等响应记录建议保留至少 7 天；超过保留期仍由状态、任务、revision 和唯一约束防止重复办理。单据创建另永久保留 creation_key，避免入库等创建请求在响应缓存过期后重复执行。
- 审批完成后，旧 revision 的请求不能覆盖新状态；任务被转交后，旧处理人的请求不能办理。
- 前端按钮防重复点击只改善体验，不能替代服务端幂等和并发保护。

### 13.5 文件与数据库一致性

文件先写临时路径，检查完成后以不可变 ID 落盘，再提交数据库附件元数据和关联。数据库不能先指向一个尚未完整保存的文件。

数据库提交失败时，已落盘但未关联的文件作为孤立文件处理，可由后台按“无任何当前/历史引用且超过 24 小时”清理。已经进入版本的附件必须保留。清理先核对引用和路径归属，不能根据用户原文件名删除文件。

入库预上传文件也受该清理规则约束；已成功绑定的文件不再是临时文件。上传失败或断网不能创建一张已确认但附件不完整的入库单。

### 13.6 启动检查与备份

- 启动时执行有版本号的数据库迁移，失败时停止提供写接口，不在旧结构上勉强运行。
- 启动后可检查运行中实例、待办、占用和业务状态不变量。发现异常应阻止受影响单据继续变更并提示管理员，不静默推断审批结果。
- 本地备份采用暂停写入、正常关闭数据库后复制完整数据目录的方式，包含数据库和原件。
- 恢复时将数据库与附件作为同一份备份恢复；恢复后核对文件摘要和基本业务不变量。
- 本版不提供一键清空正式数据；演示数据应与未来正式数据使用不同数据目录。

## 14. 台账与统计口径

### 14.1 当前状态口径

台账根据当前业务状态动态生成。日期筛选限制记录的业务日期，但不表示“还原到某一天当时的状态”。历史时点报表属于后续能力；历史审批内容通过版本查看。

| 指标 | 公式 | 排除项 |
| --- | --- | --- |
| 有效订单金额 | 当前状态为 APPROVED 的订单价税合计之和 | 草稿、退回、审批中、作废订单 |
| 已确认入库数量 | 已确认入库行数量之和 | 未确认录入、临时页面输入 |
| 已通过发票金额 | 当前状态为 APPROVED 的发票票面价税合计之和 | 待审、退回、草稿、作废 |
| 待审发票金额 | 当前状态为 IN_REVIEW 的发票票面价税合计之和 | 其他状态 |
| 已通过开票数量 | CONFIRMED 分配的数量合计 | RESERVED、RELEASED |
| 待审占用数量 | RESERVED 分配的数量合计 | CONFIRMED、RELEASED |
| 可开票数量 | 已确认入库数量 − CONFIRMED − RESERVED | 不扣减草稿或退回数量 |

不同单位不能直接相加成一个“数量总计”。订单同时包含个、箱、台时，按单位分组展示，例如“20 箱 / 3 台”；详细追溯按订单行计算。

### 14.2 进度计算

入库进度：所有订单行实收数量均为零是 NONE；所有行均等于订单数量是 FULL；其余为 PARTIAL。

开票进度：所有订单行已通过开票数量均为零是 NONE；所有行均达到订单数量是 FULL；其余为 PARTIAL。审批中的发票另显示待审占用，不计入已开票进度。

因此，某张订单只入库 60/100 且这 60 全部开票，订单仍显示“部分入库、部分开票”，同时可开票数量为零。这三个指标表达不同含义，不应互相替代。

### 14.3 综合台账

每张订单一行：订单号、供应商、采购员、创建日期、预计到货日期、当前审批状态、订单价税合计、有效订单金额、按单位汇总的入库数量、入库进度、已通过发票金额、待审金额、开票进度、单据备注。

默认展示全部未删除订单，可按状态过滤。未生效订单的“订单价税合计”可以显示原值，但“有效订单金额”为零，汇总卡只累计有效金额，避免把展示金额误计入正式统计。

查询时先按订单选出符合权限和筛选条件的集合，再分别聚合入库与发票，最后关联到订单。不能直接把“多条订单行×多次入库×多张发票”连接后求和，以免金额被乘倍放大。

### 14.4 各台账日期含义

| 台账 | 默认日期筛选字段 | 补充说明 |
| --- | --- | --- |
| 综合/采购 | 订单创建业务日期 | 选中的订单关联统计包含其全部有效下游业务，除非另加明确的下游日期条件 |
| 供应商 | 创建业务日期 | 可查看准入时间；不与创建时间混用 |
| 入库 | 入库业务日期 | 确认时间单独保留，可供审计查询 |
| 发票 | 开票日期 | 可切换按最终审核日期筛选，界面和导出必须标明当前口径 |

日期起止均包含当天，按公司业务时区处理；开票日期按日期字段查询，不强行转成 UTC 零点时间戳。

### 14.5 Excel 导出

- 输出真正的 `.xlsx` 文件，不将 CSV 更名为 Excel。
- 导出与页面使用同一个查询、权限和金额口径，包含所有符合条件的行。
- 单号、发票号码、信用代码、银行账号按文本写入，保留前导零和长数字。
- 金额写为数值并设置两位小数显示；数量按约定精度显示，附单位列。
- 备注等用户输入作为文本单元格写入，不能被解释成公式。
- 工作簿附“导出说明”：生成时间、操作人、台账类型、日期字段、筛选条件、记录数、当前状态口径。
- 发票台账包含审批状态和当前节点；默认正式视图只展示已通过记录，可切换“全部状态”，待审汇总保持单列。
- 同步导出上限设计为 10,000 行，超过时要求缩小范围并说明数量，不静默截断。异步大批量导出列入后续版本。

## 15. 配置、安全与运行要求

### 15.1 初始配置

演示环境准备六个不同用户，分别对应六种角色；至少一名启用采购主管、一名启用采购员、一名启用财务。公司税号、公司名称及仓库使用明确标记的演示资料。

公司默认时区为 Asia/Shanghai，币种 CNY。存在已提交发票后，公司名称和税号锁定，避免现有票据重新提交时身份基准突然变化。需要切换公司时使用独立账套或新数据目录，不能直接覆盖历史主体。

没有完整公司身份、合法审批人和启用仓库时，相关业务页面给出具体缺项提示，而不是等到最终审批才失败。

### 15.2 服务端安全边界

- 本地演示服务只绑定本机回环地址；局域网或公网使用应另行完成真实登录、访问控制和部署设计。
- 会话采用不可预测的服务端凭证，Cookie 使用 HttpOnly 和 SameSite；启用 HTTPS 的环境使用 Secure。
- 修改请求执行同源/CSRF 校验，不接受任意网站借浏览器会话提交审批。
- 所有 SQL 参数化；用户文字按文本显示，避免备注、文件名、XML 内容注入页面。
- 附件路径由服务端生成，下载必须经过单据授权；历史附件也不能绕过权限。
- 日志不输出完整文件内容、会话凭证或不必要的银行信息。

### 15.3 性能和可靠性目标

以下为建议验收目标，不是当前已测得的性能结论：

| 项目 | 目标 |
| --- | --- |
| 常规列表查询 | 在约定测试机器和 10,000 张业务单据量下，95% 请求≤1秒 |
| 提交/审批 | 不含文件传输，95% 请求≤2秒；确保事务完整性优先 |
| 本地并发 | 至少 5 个会话同时操作时，不发生超收、超开、重复审批 |
| 保存持久化 | 正常关闭并重启后，单据、原件、版本、待办均可恢复 |
| 异常提示 | 网络/锁冲突/校验失败保留未保存输入，不制造虚假成功 |

数据量、硬件和测量方法必须写入实现阶段的测试报告。若目标未满足，应说明实测结果及适用规模，不能只以“支持多人使用”代替验证。

## 16. 建议开发顺序与交付物

本章用于后续开发排期，本次不执行开发。

| 阶段 | 工作内容 | 阶段完成条件 |
| --- | --- | --- |
| 1 基础 | 用户、角色、公司、数据库迁移、公共单据、编号、权限 | 服务端可以区分六种角色及数据范围 |
| 2 上游业务 | 供应商与订单、主管审批、版本与附件 | 供应商退回重提通过，订单批准后生效 |
| 3 入库 | 分批收货、超量校验、不可变确认记录 | 两批入库和并发超收测试通过 |
| 4 发票 | 原件、XML、人工确认、身份查重、分配 | 完成原票到订单行和入库行的追溯 |
| 5 审批闭环 | 双审、退回、撤回、作废、恢复、转交 | 财务退回后重新走两级；所有占用状态正确 |
| 6 台账与验收 | 查询、Excel、备份、异常和权限验证 | 第 17 章关键用例通过，交付操作说明及测试记录 |

后续实际程序应随附：可运行工程、环境配置说明、版本化数据库迁移、演示数据、XML 样例、使用说明、测试结果、已知限制。不能仅交付截图或未经验证的接口清单。

## 17. 验收用例

下表是待实现后执行的验收标准，**并非已经通过的测试结果**。

| 编号 | 场景与操作 | 预期结果 |
| --- | --- | --- |
| A01 | 供应商提交后被主管退回，补充资质并重提 | 生成 V2；V1 原字段、附件及退回意见仍可查；通过后才准入 |
| A02 | 用未准入/作废供应商创建或提交订单 | 被服务端拒绝；不产生生效订单 |
| A03 | 同一信用代码重复登记供应商 | 唯一约束阻止重复；不覆盖原资料 |
| A04 | 订单为 100 个，分两次入库 60 个和 40 个 | 总入库 100，进度 FULL，保留两张入库单 |
| A05 | 已入库 60 个，再提交 41 个 | 整单失败，仍为 60，指出当前最多 40 |
| A06 | 两个会话同时争取最后 10 个入库额度 | 最多一个超出后余额的请求成功，累计不超过订单 |
| A07 | 非仓管/管理员调用确认入库接口 | 返回权限错误，不产生入库记录 |
| A08 | 上传约定 XML，核对后关联订单并确认 | 原件和解析结果保留，字段回填，人工确认后才可提交 |
| A09 | 上传不支持结构/损坏 XML | 提示具体原因，原件保留，可人工录入 |
| A10 | XML 含 DTD、外部实体或超限嵌套 | 拒绝解析，不访问外部地址，不影响其他单据 |
| A11 | PDF/OFD 上传并保存 | PDF 可预览和下载；OFD 可下载；不声称已自动识别 |
| A12 | 文件超过 20 MB，或扩展名与内容不符 | 上传失败，不产生可提交的虚假附件 |
| A13 | 同票另传 PDF，或重复新建相同销方＋号码 | 前者追加到原记录；后者被拒绝；前导零不丢失 |
| A14 | 销方税号不同于供应商，或购方不同于公司 | 提交失败，定位到冲突字段 |
| A15 | 将发票行映射到另一张订单的商品行 | 提交失败，不产生跨订单分配 |
| A16 | 同一订单行被多个发票行引用，合计超过余额 | 按汇总需求拒绝，不能逐行重复使用旧余额 |
| A17 | 入库 60＋40，第一批已有 30，新票开 50 | 自动分配第一批 30、第二批 20，可追溯原入库行 |
| A18 | 采购通过后进入财务 | 状态仍审批中，占用连续保留，只有财务待办 |
| A19 | 财务退回，修改金额后重提 | 旧占用释放；新版本从采购审核开始；旧财务意见保留 |
| A20 | A 退回释放数量，B 占用后 A 再提交 | A 根据最新余额被拒绝或重新分配，不抢占 B |
| A21 | 两张发票并发争取同一入库余额 | 最终有效分配不超过入库，失败方不留下半个流程 |
| A22 | 提交人自审，或采购与财务为同一人 | 服务端拒绝；改前端角色显示无法绕过 |
| A23 | 无合法主管/采购/财务人员 | 提交失败并说明缺项；不产生空处理人待办 |
| A24 | 单价/税率不同，无差异说明 | 禁止提交；补说明后可进入双审，审核人可见差异 |
| A25 | 金额不等于行合计，或价税差额超过 0.01 | 禁止提交；合法 ±0.01 舍入差额单独保存 |
| A26 | 人工确认后修改业务字段或附件 | 确认失效，必须针对新内容重新确认 |
| A27 | 审批中编辑字段或追加附件 | 后端拒绝；追加评论允许且不覆盖意见 |
| A28 | 有入库的订单尝试作废 | 被拒绝，入库和关联发票保持完整 |
| A29 | 财务作废已通过发票 | 从当前正式金额移除，数量释放，旧审批保持已通过历史 |
| A30 | 另建已作废发票同号，随后管理员恢复原票 | 新建被拒绝；恢复原票不直接占用，完整重审后才重新计入 |
| A31 | 对已处理任务重复点击或重放请求 | 返回原幂等结果或明确冲突，不重复意见、任务和数量 |
| A32 | 返回响应前连接中断，以相同幂等键重试 | 若已提交返回原成功结果；若未提交可正常重试 |
| A33 | 旧页面保存覆盖新页面修改 | revision 冲突，不能静默覆盖 |
| A34 | 管理员转交待办后旧处理人审批 | 旧处理人被拒绝；新处理人及转交原因可追溯 |
| A35 | 直接访问其他人不可见的单据/附件/导出 | 被拒绝，不泄露完整业务或原件 |
| A36 | 订单多明细、多入库、多发票联合统计 | 订单金额不被重复累计，已通过/待审分列正确 |
| A37 | 同一订单商品单位为箱和台 | 数量按单位展示，不显示没有意义的合计数 |
| A38 | 按日期和状态筛选后导出超过一页数据 | 导出完整筛选范围，日期口径一致，长号码按文本保留 |
| A39 | 保存后正常重启，再恢复完整备份 | 单据、附件、版本、待办、占用关系一致 |
| A40 | 在提交事务的中间步骤模拟失败 | 无审批中孤单、无孤立占用、无半张入库；文件孤立项可按规则清理 |

### 17.1 一条完整演示脚本

1. 经办人创建“演示供应商甲”，上传资质，提交主管。
2. 主管退回“请补充开户资料”；经办人补充后重提，主管通过。
3. 经办人创建采购订单：商品 A 100 个，未税单价 100 元，税率 13%；主管通过，订单价税合计 11,300 元。
4. 仓管分两次入库 60 个、40 个；尝试再入库 1 个，验证超量拦截。
5. 经办人导入 50 个 A 商品的约定 XML，未税金额 5,000、税额 650、价税合计 5,650；关联订单并人工确认，提交发票 V1。
6. 采购通过；财务退回“请核对商品说明”。系统释放 50 个占用。
7. 经办人修改说明并重新确认，提交 V2；采购、财务分别通过。
8. 综合台账显示有效订单 11,300、已通过发票 5,650、待审 0；已入库 100、已开票 50、可开票 50。
9. 详情可看到 V1/V2、原始 XML、修改记录、退回原因和两轮审批；尝试同号新建，验证重复拦截。

## 18. 后续边界与设计交接

本版已明确业务闭环，但以下能力不能在开发时被默认当作已包含：

- 真实多人登录、企业身份接入、离职业务移交及更细的字段权限。
- 已批准供应商/订单的变更审批、入库冲销、退货、红字及付款。
- 多家公司/账套、税务身份有效期管理、历史时点台账。
- 真实票据 XML 样本兼容、OFD 内嵌渲染、OCR、验真和税务外部系统连接。
- WarmFlow、MySQL、容器化和公网部署；需要时单独设计升级与迁移方案。

开发应以本文的状态、权限、字段精度和事务边界为统一实现口径。若业务负责人修改退回占用、作废恢复、唯一身份或金额容差，必须同步更新数据库约束、接口、页面提示和验收用例，不能只改一处说明。

## 附录 A：约定 XML 样例

以下仅为接口与解析器设计样例，不是实际税务发票，也不是程序实现。导入后仍须选择订单、映射订单行、人工核对再提交。名称和税号须与演示公司及演示供应商配置一致。

```xml
<?xml version="1.0" encoding="UTF-8"?>
<DemoInvoice schemaVersion="1.0">
  <Header>
    <InvoiceNumber>00000000000000000001</InvoiceNumber>
    <InvoiceType>DEMO_GENERAL</InvoiceType>
    <IssueDate>2026-09-06</IssueDate>
    <Currency>CNY</Currency>
  </Header>
  <Seller>
    <Name>演示供应商甲</Name>
    <TaxId>DEMOSELLER00000001</TaxId>
  </Seller>
  <Buyer>
    <Name>演示采购公司</Name>
    <TaxId>DEMOBUYER000000001</TaxId>
  </Buyer>
  <Lines>
    <Line>
      <Name>商品 A</Name>
      <Specification>演示规格</Specification>
      <Unit>个</Unit>
      <Quantity>50.0000</Quantity>
      <UnitPrice>100.000000</UnitPrice>
      <TaxRate>0.1300</TaxRate>
      <Amount>5000.00</Amount>
      <TaxAmount>650.00</TaxAmount>
    </Line>
  </Lines>
  <Totals>
    <Amount>5000.00</Amount>
    <TaxAmount>650.00</TaxAmount>
    <TotalAmount>5650.00</TotalAmount>
  </Totals>
</DemoInvoice>
```

发票类型建议使用业务枚举 `GENERAL 普通发票 / SPECIAL 专用发票 / DEMO_GENERAL 演示普通票`，可增加受控类型但不改变唯一键；界面不得把演示类型显示成真实验真结果。

## 附录 B：典型请求与失败响应

下列内容说明接口契约，不包含可运行代码。

提交已有发票：`POST /api/v1/documents/{id}/submit`，请求头包含 `Idempotency-Key` 和 `If-Match`。提交接口直接读取服务端已保存、已确认的单据，不再接受另一份完整票面覆盖它。

```json
{
  "data": {
    "documentId": "invoice-id",
    "number": "INV-20260906-00001",
    "versionNo": 2,
    "revision": 9,
    "approvalStatus": "IN_REVIEW",
    "currentNode": "PURCHASE",
    "instanceId": "instance-id"
  }
}
```

余额不足时：

```json
{
  "error": {
    "code": "INVOICE_QUANTITY_EXCEEDED",
    "message": "商品 A 当前可开票 20 个，本次申请 30 个。",
    "fields": [
      {
        "orderLineId": "order-line-id",
        "requestedQuantity": "30.0000",
        "availableQuantity": "20.0000",
        "unit": "个"
      }
    ]
  },
  "requestId": "request-id"
}
```

系统应保留用户表单输入，提示检查其他待审发票或调整数量，不自动减少到 20 个后替用户提交。
