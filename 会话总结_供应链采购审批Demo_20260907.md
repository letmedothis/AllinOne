# 供应链采购审批 Demo：本次会话总结

## 1. 原始需求

基于《供应链采购审批与票据台账系统_详细设计_V1.0.md》，在当前 AllinOne 项目中实现一个供应链采购审批与票据台账 Demo。

原需求文档已从：

`/home/xu/project/demo/供应链采购审批与票据台账系统_详细设计_V1.0.md`

复制到项目根目录：

`供应链采购审批与票据台账系统_详细设计_V1.0.md`

## 2. 技术方案决策

当前项目技术栈为 Spring Boot 3 + MyBatis + Vue 3 + TypeScript + Element Plus + MySQL，因此没有采用需求文档中建议的 Node.js + SQLite 独立工程。

当前采用：

- 新增独立后端模块 `allinone-supply`
- 后端沿用 Controller → Service → Mapper 分层
- 前端页面放在 `allinone-typescript/src`
- 审批流先采用应用内固定流程，不接入 WarmFlow
- 数据库沿用现有 MySQL 业务脚本

## 3. 已完成内容

本轮继续补齐：

- 修正发票明细 MyBatis 映射，补齐发票草稿查询字段，避免提交时明细对象为空或快照字段缺失。
- 新增 `attachment_files`、`document_attachments` 附件归档表及幂等升级脚本。
- 新增单据附件上传/查询后端接口，限制为创建人/管理员且仅 DRAFT、RETURNED 状态可上传，并记录原名、路径、媒体类型、大小、SHA-256 和上传人。
- 供应商提交校验兼容新附件绑定表和现有 Demo 的 `attachment_paths` 字段；前端 API 已提供通用附件接口。
- 修正综合台账 Excel 导出：金额统一从数据库分转换为元，页面查询仍保持分字段，避免导出数值放大 100 倍。

本轮验证：`git diff --check` 通过；Maven 离线编译因本地缺少 Spring Boot 依赖且外网仓库 DNS 不可用而未进入编译阶段。

### 3.1 后端模块

新增模块：

`allinone-supply`

已接入根 `pom.xml` 和 `allinone-admin/pom.xml`，应用会自动扫描 `com.allinone` 包。

新增领域枚举：

- `SupplyDocumentType`
- `SupplyApprovalStatus`
- `SupplyWorkflowNode`

### 3.2 数据库基础表

已同步写入：

- `sql/allinone_biz.sql`
- `sql/allinone_biz_update.sql`

当前已增加：

- `company_profile`
- `warehouses`
- `number_sequences`
- `documents`
- `suppliers`
- `purchase_orders`
- `purchase_order_lines`
- `workflow_configs`
- `document_versions`
- `workflow_instances`
- `workflow_tasks`
- `audit_events`

供应商表额外增加了 Demo 版附件字段：

`attachment_paths varchar(4000)`

### 3.3 供应商模块

已实现：

- 供应商列表
- 供应商详情
- 新建供应商
- 编辑草稿/退回供应商
- 创建人数据隔离
- 税号格式校验
- 供应商单号生成
- 乐观锁校验
- 提交主管审批
- 版本快照
- 审批任务
- 审计事件
- 资质附件必填校验

主要接口：

- `GET /supply/suppliers/list`
- `GET /supply/suppliers/{documentId}`
- `POST /supply/suppliers`
- `PUT /supply/suppliers`
- `POST /supply/suppliers/{documentId}/submit`

### 3.4 采购订单模块

已实现：

- 采购订单列表
- 订单详情
- 新增订单草稿
- 编辑订单草稿/退回单
- 多行商品明细
- 数量、单价、税率校验
- 未税金额、税额、价税合计服务端计算
- 已准入供应商校验
- 提交主管审批
- 订单版本快照和审计记录

主要接口：

- `GET /supply/orders/list`
- `GET /supply/orders/{id}`
- `POST /supply/orders`
- `PUT /supply/orders`
- `POST /supply/orders/{id}/submit`

### 3.5 审批中心

已实现当前主管节点的审批闭环：

- 待办列表
- 审批通过
- 审批退回
- 办理人校验
- 任务状态校验
- 任务版本校验
- 单据版本校验
- 流程实例关闭
- 审计事件记录

主要接口：

- `GET /supply/approvals/pending`
- `POST /supply/approvals/approve`
- `POST /supply/approvals/reject`

当前审批流程：

`经办人 → 采购主管`

### 3.6 前端页面

已新增：

- 供应商管理：`src/views/supply/supplier/index.vue`
- 采购订单：`src/views/supply/order/index.vue`
- 审批中心：`src/views/supply/approval/index.vue`
- 供应链 API：`src/api/supply.ts`
- 前端类型：`src/types/api/supply/index.ts`

供应商页面支持：

- 查询
- 新增
- 编辑
- 提交审批
- 上传资质附件

采购订单页面支持：

- 查询
- 新增
- 编辑
- 多行商品明细
- 保存草稿
- 提交审批

审批页面支持：

- 查看当前用户待办
- 通过
- 退回并填写原因

### 3.7 菜单和权限

已在 `sql/allinone_menu.sql` 增加：

- 供应链管理
- 供应商管理
- 采购订单
- 审批中心
- 对应查询、新增、修改、审批权限

权限前缀：

- `supply:supplier:*`
- `supply:order:*`
- `supply:approval:*`

## 4. 审批人配置要求

提交供应商或采购订单前，必须在 `workflow_configs` 中配置：

- `flow_type = 'SUPPLIER_ONBOARDING'`
- `flow_type = 'PURCHASE_ORDER_APPROVAL'`

`supervisor_candidates` 保存逗号分隔的用户 ID，例如：

`2,3,4`

系统会选择第一个候选人作为主管审批人，并拒绝提交人自审。

如果没有合法候选人，提交会返回配置缺失错误。

## 5. 当前 Demo 版限制

以下功能尚未完成：

- 真实演示用户和角色初始化
- 系统设置中的审批候选人配置页面
- 采购员/供应商选择器，目前部分字段使用 ID 输入
- 订单审批详情页
- 供应商详情页
- 独立附件表和文件摘要
- 单据级附件下载鉴权
- 分批入库
- 发票 XML/PDF/OFD 解析
- 发票采购、财务两级审批
- 台账中心和 Excel 导出
- 撤回、作废、恢复、任务转交
- 幂等键和完整 If-Match 请求头处理
- 完整数据权限范围
- 完整版本字段差异审计

当前供应商附件采用 `attachment_paths` 字段保存通用上传接口返回的文件路径，仅适合本地 Demo，不等同于需求文档中设计的不可变附件模型。

## 6. 验证情况

已通过：

- `git diff --check`
- 前端 `npm run test:contracts`

未完成：

- 前端生产构建：当前环境缺少 `vite`，需要先执行 `npm install`
- Maven 编译：此前因依赖下载失败未完成，后续使用可用的 Maven 镜像完成编译验证
- MySQL 实际建表验证：当前会话未连接数据库

## 7. 下一步建议

建议按以下顺序继续：

1. 初始化 6 个演示角色用户和审批候选人配置。
2. 增加用户选择器、供应商选择器，去掉页面中的手工 ID 输入。
3. 完善供应商详情、审批详情和订单详情页面。
4. 实现分批入库及数量并发校验。
5. 实现发票登记、约定 XML v1 解析、订单行和入库行自动分配。
6. 将供应商附件升级为 `attachment_files`、`document_attachments`、`version_attachments` 三类表。
7. 实现综合台账、各类台账和 Excel 导出。
8. 依照需求文档第 17 章执行验收用例。

## 9. 本次后续进展

在总结文件创建后，又完成了：

- 分批入库基础表 `receipts`、`receipt_lines`
- 入库准备接口 `GET /supply/receipts/prepare/{orderId}`
- 入库确认接口 `POST /supply/receipts/confirm`
- 已通过订单、启用仓库、入库日期和仓管权限校验
- 入库余额事务校验，防止超收
- 入库管理页面 `src/views/supply/receipt/index.vue`
- 发票基础表 `invoices`、`invoice_lines`、`invoice_identities`、`invoice_allocations`
- 约定 `DemoInvoice schemaVersion=1.0` 安全解析器
- 发票 XML 解析接口 `POST /supply/invoices/parse-xml`
- 发票 XML 解析页面 `src/views/supply/invoice/index.vue`
- 供应链菜单中的入库管理和发票管理入口

当前发票模块仍未完成正式登记、订单/入库行分配和采购/财务双级审批；XML 解析结果必须人工核对，不能视为税务验真。

补充进展：已增加发票登记基础模型、发票身份查重、按入库批次顺序自动生成 RESERVED 分配，以及发票提交时的采购节点；采购审批通过后会根据流程配置创建财务节点待办。发票页面目前仍主要用于 XML 解析展示，完整票面登记交互待继续完善。

最新进展：发票提交已改为服务端读取已保存草稿，并在提交事务内重新按入库批次分配数量；避免客户端直接提交分配结果。发票采购审核通过后创建财务审批任务，财务通过后才完成最终审批。新增发票基础登记接口与发票管理菜单入口。

最新进展：发票页面已支持 XML 结果回填、人工补充订单 ID/订单行 ID、保存发票草稿并提交采购审核。发票提交服务端会重新读取草稿，按当前入库余额生成分配，避免使用客户端篡改的金额或分配结果。

最新进展：新增供应链工作台统计接口和页面，按当前用户可见范围展示有效订单金额、已通过发票金额、待审发票金额和待办任务数；采购订单页面已改为选择已准入供应商和启用系统用户。

最新进展：综合台账新增真正的 `.xlsx` 导出接口 `POST /supply/ledger/export`，导出复用页面筛选和数据权限范围，金额按数值字段输出。前端导出按钮仍待接入。

最新进展：综合台账已接入前端 Excel 导出按钮；新增审批流程配置接口和管理员页面，可维护发票采购/财务及订单、供应商主管候选人，配置按版本保存并只影响后续提交。

最新进展：增加 `sql/supply_demo_seed.sql` 和 `allinone-supply/README.md`，用于初始化演示公司、仓库和审批流程。审批用户通过 SQL 会话变量传入，不创建或覆盖现有系统用户。

## 8. 重要实现原则

- 审批状态和当前节点分开保存。
- 业务字段由服务端校验，不能只依赖前端按钮控制。
- 通过审批不代表付款。
- 已提交版本不可覆盖，只能生成新版本。
- 订单金额必须由服务端重新计算。
- 审批任务必须绑定具体版本。
- 退回和撤回后应释放后续业务占用。
- 供应链台账必须避免订单行、入库行、发票行多表连接造成金额重复统计。

## 10. 本轮问题修复与最终验证（2026-09-07）

本轮继续修复了以下问题：

- 修复发票创建时创建人字段绑定错误，改为服务端真实创建人。
- 发票提交增加至少一个数据库附件绑定校验；供应商页面改用数据库附件绑定后再提交。
- 统一税率口径为比例值，例如 `0.1300`，订单、发票后端计算和两个前端表单保持一致。
- 增加数量、单价、税率上限及小数精度校验，避免数据库静默截断。
- 审批候选人增加启用状态、角色、提交人与审批人互斥校验，并支持跳过无效候选人继续选择。
- 修复采购主管、财务、全局业务角色的详情、附件、评论和工作台数据范围判断。
- 阻止通用作废接口误处理入库单；供应商详情兼容隐藏路由的 `id` 参数。
- 补齐存量库发票确认人和确认时间字段的幂等升级脚本。

最终验证结果：

- `mvn -o -Dmaven.repo.local=/tmp/allinone-m2 -pl allinone-supply -am -DskipTests compile` 通过。
- `npm run test:contracts` 通过。
- 所有供应链 MyBatis XML 可解析，`git diff --check` 通过。
- 前端生产构建仍因当前环境没有安装 `node_modules/vite` 无法执行；未擅自修改依赖锁文件。
- 尚未连接真实 MySQL，因此数据库迁移和完整浏览器验收仍需在目标环境执行。
- 最后又补充了评论接口的单据存在性校验，以及审批待办结果中的提交人 ID 映射，避免全局角色访问不存在单据和财务候选人互斥校验失效。
